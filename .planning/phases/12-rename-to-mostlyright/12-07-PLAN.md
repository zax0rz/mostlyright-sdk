---
phase: 12-rename-to-mostlyright
plan: 07
type: execute
wave: 7
depends_on: [12-06]
files_modified:
  - .planning/phases/12-rename-to-mostlyright/README.md  # NEW
  - .planning/STATE.md  # closeout section
autonomous: true
requirements:
  - RENAME-08
  - RENAME-09
  - RENAME-10
tags:
  - rename
  - parity-gate
  - closeout
  - docs
must_haves:
  truths:
    - "`uv run pytest -q` (INCLUDING @pytest.mark.live parity fixtures) exits 0 — parity gate byte-equivalent under new module name `from mostlyright import research`"
    - "`CI=1 pnpm -r run test` exits 0 (full TS suite)"
    - "`python -c 'import mostlyright; print(mostlyright.__version__)'` works after fresh `uv sync`"
    - "`node -e \"console.log(require('@mostlyrightmd/core'))\"` works after fresh `pnpm install`"
    - "Final verification grep returns 0: `grep -rn 'tradewinds' packages/ packages-ts/ tests/ scripts/ docs/ README.md CLAUDE.md .github/workflows/ --include='*.py' --include='*.ts' --include='*.tsx' --include='*.json' --include='*.toml' --include='*.md' --include='*.yml' --include='*.yaml' | grep -v 'mostlyright_v1\\|MostlyRight\\|monorepo-v0.14.1\\|mostlyright==0.14.1\\|tradewinds-legacy\\|github.com/helloiamvu/tradewinds\\|\\.tradewinds\\|TW_HOSTED_URL\\|\\.planning/' | wc -l == 0`"
    - "`pnpm codegen` produces zero diff (idempotency re-verified post-W6)"
    - "Phase 12 README documents operator OP1-OP4 + post-merge follow-ups + v0.3 back-compat removal timeline"
    - "STATE.md closeout section appended in standard Phase-N closeout format"
  artifacts:
    - path: ".planning/phases/12-rename-to-mostlyright/README.md"
      provides: "Phase 12 closeout doc: rename history, operator pre-flight checklist, orphaned name registry, v0.3 deprecation timeline, PR-description operator-confirmation template"
      contains: "## Operator pre-flight (OP1-OP4); ## Post-merge follow-ups; ## v0.3 cleanup"
    - path: ".planning/STATE.md"
      provides: "Phase 12 closeout section appended in standard Phase-N format"
  key_links:
    - from: ".planning/phases/12-rename-to-mostlyright/README.md"
      to: "PR description"
      via: "4-line operator-confirmation checklist (OP1-OP4) operator pastes into the merged-vision → main PR"
---

<objective>
W7 — Final wave. Parity-gate pre-flight + full live test run + STATE.md closeout + Phase 12 README. This is the LAST in-tree wave; W8/W9 are operator-gated (out of plan scope). The wave proves zero regression at every layer (Python live + parity fixtures, TS, schema codegen, wheel build, smoke imports), writes the closeout README with operator-confirmation lines for the PR description, and updates STATE.md.

Purpose: Prove the rename is complete + correct + parity-clean. Document the operator follow-ups in a single place reviewers can audit.

Output: All gates green. README documents rename history. STATE.md reflects Phase 12 closeout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-rename-to-mostlyright/12-CONTEXT.md
@.planning/phases/12-rename-to-mostlyright/12-RESEARCH.md
@.planning/STATE.md
@CLAUDE.md
@tests/test_parity.py
@.planning/phases/12-rename-to-mostlyright/12-01-SUMMARY.md
@.planning/phases/12-rename-to-mostlyright/12-02-SUMMARY.md
@.planning/phases/12-rename-to-mostlyright/12-03-SUMMARY.md
@.planning/phases/12-rename-to-mostlyright/12-04-SUMMARY.md
@.planning/phases/12-rename-to-mostlyright/12-05-SUMMARY.md
@.planning/phases/12-rename-to-mostlyright/12-06-SUMMARY.md

# OPERATOR PRE-FLIGHT CHECKLIST (REQUIRED in PR description per RENAME-09):
#   - [ ] OP1: `mv ~/Documents/GitHub/mostlyright ~/Documents/GitHub/mostlyright-legacy` (confirmed)
#   - [ ] OP2: 3 PyPI pending publishers registered (mostlyright / mostlyright-weather / mostlyright-markets, bound to repo + release.yml + env pypi) (confirmed)
#   - [ ] OP3: @mostlyrightmd npm scope claimed (confirmed)
#   - [ ] OP4: 4 npm OIDC pending publishers registered (@mostlyrightmd/{core,weather,markets} + unscoped meta mostlyright, bound to repo + release-ts.yml + env npm) (confirmed)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parity gate + full live test run + smoke imports</name>
  <files>(verification only; no file modifications)</files>
  <read_first>
    tests/test_parity.py (the 5 fixture byte-equivalence test — the HARD GATE per CLAUDE.md),
    .planning/phases/12-rename-to-mostlyright/12-CONTEXT.md (§Test-pass gate semantics — Parity green required for W7)
  </read_first>
  <action>
    Step 1 — fresh uv sync to flush stale developer venv state (per RESEARCH §Pitfall 11):
      rm -rf .venv
      uv sync --all-packages
      uv pip list | grep -iE 'tradewinds|mostlyright'   # expect mostlyright* listed; NO tradewinds*

    Step 2 — Python parity gate (HARD GATE per CLAUDE.md — Sprint 0 only ships if green):
      uv run pytest tests/test_parity.py -q
    All 5 byte-equivalent fixtures must be GREEN reading via `from mostlyright import research`. This is the hardest test in the suite — if it fails, Phase 12 does NOT ship; revert or debug.

    Step 3 — full live test run (includes @pytest.mark.live):
      uv run pytest -q
    All non-skipped tests pass. Live tests may skip in dev environments without network — verify they're skipping for the right reason, not failing.

    Step 4 — full TS test + typecheck:
      pnpm -r run typecheck   # exit 0
      CI=1 pnpm -r run test   # exit 0

    Step 5 — smoke imports (matches CONTEXT.md success criteria #6 + #7):
      python -c "import mostlyright; print(mostlyright.__version__)"
      python -c "import mostlyright.weather; import mostlyright.markets"
      python -c "from mostlyright import research; print(research)"
      node -e "console.log(Object.keys(require('@mostlyrightmd/core')).length)"

    Step 6 — fresh wheel build + 3-distro produce check:
      rm -rf dist/
      uv build --all-packages
      ls dist/mostlyright-*.whl dist/mostlyright_weather-*.whl dist/mostlyright_markets-*.whl
      unzip -p dist/mostlyright-*.whl '*/METADATA' | grep '^Name: mostlyright$'
      unzip -p dist/mostlyright_weather-*.whl '*/METADATA' | grep '^Name: mostlyright-weather$'
      unzip -p dist/mostlyright_markets-*.whl '*/METADATA' | grep '^Name: mostlyright-markets$'

    Step 7 — re-run pnpm codegen idempotency check:
      pnpm codegen
      git status --porcelain packages-ts/*/src/data/generated/ schemas/json/ | wc -l   # expect 0

    Step 8 — FINAL VERIFICATION GREP (the success criterion grep from RESEARCH §State of the Art):
      grep -rEn 'tradewinds' \
        packages/ packages-ts/ tests/ scripts/ docs/ README.md CLAUDE.md .github/workflows/ \
        --include='*.py' --include='*.ts' --include='*.tsx' --include='*.json' \
        --include='*.toml' --include='*.md' --include='*.yml' --include='*.yaml' \
        | grep -v 'mostlyright_v1\|MostlyRight\|monorepo-v0\.14\.1\|mostlyright==0\.14\.1\|tradewinds-legacy\|github\.com/helloiamvu/tradewinds\|\.tradewinds\|TW_HOSTED_URL\|\.planning/'
      # Expected output: 0 lines.
  </action>
  <verify>
    <automated>
    # 1. Parity gate (the HARD GATE)
    uv run pytest tests/test_parity.py -q  # exit 0
    # 2. Full Python suite including @pytest.mark.live
    uv run pytest -q  # exit 0
    # 3. TS suite + typecheck
    pnpm -r run typecheck  # exit 0
    CI=1 pnpm -r run test  # exit 0
    # 4. Smoke imports
    python -c "import mostlyright; from mostlyright import research"
    node -e "require('@mostlyrightmd/core')"
    # 5. Wheels build with new names
    ls dist/mostlyright-*.whl dist/mostlyright_weather-*.whl dist/mostlyright_markets-*.whl > /dev/null
    # 6. Codegen idempotent
    pnpm codegen >/dev/null && git status --porcelain packages-ts/*/src/data/generated/ schemas/json/ | wc -l | grep -q '^0$'
    # 7. FINAL VERIFICATION GREP — 0 unauthorized tradewinds refs anywhere in tracked source
    grep -rEn 'tradewinds' packages/ packages-ts/ tests/ scripts/ docs/ README.md CLAUDE.md .github/workflows/ --include='*.py' --include='*.ts' --include='*.tsx' --include='*.json' --include='*.toml' --include='*.md' --include='*.yml' --include='*.yaml' | grep -v 'mostlyright_v1\|MostlyRight\|monorepo-v0\.14\.1\|mostlyright==0\.14\.1\|tradewinds-legacy\|github\.com/helloiamvu/tradewinds\|\.tradewinds\|TW_HOSTED_URL\|\.planning/' | wc -l  # expect 0
    </automated>
  </verify>
  <done>
    Parity gate GREEN. Full Python + TS suites GREEN. Smoke imports work. Wheels build under new names. Codegen idempotent. Final verification grep returns 0 unauthorized refs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write Phase 12 README with operator-confirmation template</name>
  <files>.planning/phases/12-rename-to-mostlyright/README.md (NEW)</files>
  <read_first>
    .planning/phases/12-rename-to-mostlyright/12-CONTEXT.md (operator pre-flight OP1-OP4 + post-merge follow-ups),
    .planning/phases/12-rename-to-mostlyright/12-RESEARCH.md (§Open Q3 — recommended 4-line operator-confirmation checklist for PR description),
    .planning/STATE.md (review recent Phase-N closeout README format — Phase 10 has similar shape)
  </read_first>
  <action>
    Write `.planning/phases/12-rename-to-mostlyright/README.md`:

    ```markdown
    # Phase 12: Rename `tradewinds` → `mostlyright`

    **Status:** code-complete; operator gates pending
    **Closeout:** 2026-05-25 (or whenever this wave commits)
    **Branch:** feature branch off `merged-vision`; final PR `merged-vision` → `main`

    ## What this phase shipped

    Mechanical end-to-end rename of every in-repo identifier from `tradewinds` to `mostlyright`. ZERO behavior change. ZERO test regressions.

    - 3 PyPI distribution names renamed: `tradewinds` / `tradewinds-weather` / `tradewinds-markets` → `mostlyright` / `mostlyright-weather` / `mostlyright-markets`
    - 5 npm package names renamed: `@tradewinds/{codegen,core,weather,markets}` + unscoped meta `tradewinds` → `@mostlyrightmd/*` + unscoped meta `mostlyright`
    - 3 Python source directories renamed via `git mv` (blame preserved)
    - ~1074 Python `from tradewinds` / `import tradewinds` rewrites (Wave 2)
    - ~61 TypeScript `from "@tradewinds/...` rewrites (Wave 3)
    - Cache env var `TRADEWINDS_CACHE_DIR` → `MOSTLYRIGHT_CACHE_DIR` with one-release back-compat shim emitting `DeprecationWarning` (Wave 4)
    - Default cache path `~/.tradewinds/cache/v1/` → `~/.mostlyright/cache/v1/`
    - ~85 mentions in `docs/*.md` + ~20 in `CLAUDE.md` + root `README.md` rewritten (Wave 5)
    - 10 CI workflow files + 1 path-filters JSON rewritten with FILENAMES PRESERVED (Wave 6)
    - Schema `$id` URL `tradewinds.dev` → `mostlyright.dev` (19 occurrences across schemas + validators + 2 spec files); test assertion updated in lockstep

    ## What this phase did NOT do (out of plan scope)

    - **GitHub repo rename** — `helloiamvu/tradewinds` stays. In-tree URLs still point at this repo. Operator may rename out-of-band later.
    - **Old PyPI / npm package cleanup** — `tradewinds*` / `@tradewinds/*` are orphaned on the registries. Operator handles transfer or deletion.
    - **`.planning/` archive prose rewrites** — ~4858 mentions preserved as historical audit trail.
    - **`MostlyRight*` deprecation alias removal** — those are CORE-04 deprecation shims from the `mostly-light` migration; scheduled for v0.3.
    - **`mostlyright_v1` parser_name enum** — LINEAGE-01 schema literal; STAYS.
    - **`mostlyright==0.14.1` parity citations** — historical fact; STAYS.
    - **`monorepo-v0.14.1/src/mostlyright/...` lift-source paths** — provenance citations; STAY.

    ## Operator pre-flight (REQUIRED BEFORE PR MERGE)

    The following manual steps cannot be automated from the worktree. Operator confirms by replacing `[ ]` with `[x]` in the PR description before merge:

    - [ ] **OP1**: `mv ~/Documents/GitHub/mostlyright ~/Documents/GitHub/mostlyright-legacy` (confirmed)
    - [ ] **OP2**: 3 PyPI pending publishers registered (`mostlyright`, `mostlyright-weather`, `mostlyright-markets`, bound to repo + `release.yml` + env `pypi`) (confirmed)
    - [ ] **OP3**: `@mostlyrightmd` npm scope claimed on npmjs.com (confirmed)
    - [ ] **OP4**: 4 npm OIDC pending publishers registered (`@mostlyrightmd/core`, `@mostlyrightmd/weather`, `@mostlyrightmd/markets`, unscoped meta `mostlyright`, bound to repo + `release-ts.yml` + env `npm`) (confirmed)

    OP2-OP4 are critical: post-merge `release.yml` / `release-ts.yml` runs will fail with "trusted publisher not found" if NEW publishers are not registered for the renamed names BEFORE a release tag fires. OP1 prevents Python import-path collision with the legacy `~/Documents/GitHub/mostlyright` checkout (Python's `sys.path[0] = cwd` injection — see RESEARCH §Pitfall 9).

    ## Post-merge follow-ups (operator)

    After PR merges to `main`:

    1. Tag `v0.2.0` (or appropriate Python release tag) to publish renamed PyPI distributions.
    2. Tag `vts-0.2.0` (or appropriate TS release tag) to publish renamed npm packages.
    3. Run `pnpm changeset` + `pnpm changeset version` to land the renamed-package version bump PR per `.changeset/config.json` `fixed` group.
    4. Optional: transfer or delete orphaned `tradewinds*` PyPI distros + `@tradewinds/*` npm packages.
    5. Update GitHub repo settings if/when renaming `helloiamvu/tradewinds` → `helloiamvu/mostlyright` (out-of-band; in-tree URLs follow).

    ## Cache back-compat — one-release deprecation window (v0.3 removal)

    `mostlyright._internal._cache_dir.resolve_cache_dir()` reads:
    1. `MOSTLYRIGHT_CACHE_DIR` (canonical)
    2. `TRADEWINDS_CACHE_DIR` (legacy + `DeprecationWarning`)
    3. Default `~/.mostlyright/cache/v1/`

    **v0.3 will remove the `TRADEWINDS_CACHE_DIR` branch.** Users have one full v0.2.x release to migrate. Migration: `mv ~/.tradewinds ~/.mostlyright` (byte-equivalent parquet, no schema change). See `docs/cache-migration.md`.

    ## Developer post-merge step

    Existing developer venvs have `tradewinds*` editable installs registered in their `site-packages/`. Run after merge:

    ```bash
    rm -rf .venv
    uv sync --all-packages
    uv pip list | grep -iE 'tradewinds|mostlyright'  # expect only mostlyright*
    ```

    ## Test count baseline (zero regression)

    | Suite | Pre-Phase-12 baseline | Post-Phase-12 | Delta |
    |---|---|---|---|
    | Python non-live | 1971 (per STATE.md Phase 10 closeout) | (1971 + 3 new back-compat tests) | +3 (test_cache_env_back_compat.py) |
    | Python live (incl. parity fixtures) | 5 parity + N live | byte-equivalent under `from mostlyright import research` | 0 |
    | TS workspace | 1323 (per STATE.md Phase 10 closeout) | 1323 | 0 |

    ## Files added in Phase 12

    - `packages/core/src/mostlyright/_internal/_cache_dir.py` (back-compat shim)
    - `packages/core/tests/test_cache_env_back_compat.py` (3-test resolve_cache_dir coverage)
    - `scripts/_phase12_rename.py` (Python import-line rewriter)
    - `scripts/_phase12_rename_ts.py` (TS import rewriter)
    - `docs/cache-migration.md` (user-side migration guide)
    - `.planning/phases/12-rename-to-mostlyright/12-CONTEXT.md` (locked decisions)
    - `.planning/phases/12-rename-to-mostlyright/12-RESEARCH.md` (audit + recommendations)
    - `.planning/phases/12-rename-to-mostlyright/12-01-PLAN.md` through `12-07-PLAN.md`
    - `.planning/phases/12-rename-to-mostlyright/12-01-SUMMARY.md` through `12-07-SUMMARY.md`
    - This README

    ## Review

    Per `.planning/REVIEW-DISCIPLINE.md`, Phase 12 is a NEVER-SKIP path (touches `pyproject.toml` dependency floors + CI workflows + schema codegen + parity-adjacent paths). The merged-vision → main PR runs the 3-reviewer parallel dispatch (codex `high` + python-architect + ts-architect) with max 5 iterations per user override. `[review-skip: trivial]` does NOT apply.
    ```

    Save to `.planning/phases/12-rename-to-mostlyright/README.md`.
  </action>
  <verify>
    <automated>
    # 1. README exists
    test -f .planning/phases/12-rename-to-mostlyright/README.md
    # 2. Operator checklist present (4 OP items)
    grep -E '^- \[ \] \*\*OP[1-4]\*\*' .planning/phases/12-rename-to-mostlyright/README.md | wc -l  # expect 4
    # 3. v0.3 deprecation timeline documented
    grep -c 'v0.3' .planning/phases/12-rename-to-mostlyright/README.md  # expect >=2
    # 4. Cache migration cross-link
    grep -c 'docs/cache-migration.md' .planning/phases/12-rename-to-mostlyright/README.md  # expect >=1
    # 5. NEVER-SKIP per REVIEW-DISCIPLINE documented
    grep -c 'NEVER-SKIP\|never-skip' .planning/phases/12-rename-to-mostlyright/README.md  # expect >=1
    </automated>
  </verify>
  <done>
    Phase 12 README written with rename history, operator OP1-OP4 checklist (PR-description template), post-merge follow-ups, v0.3 back-compat removal timeline, developer post-merge step, test count baselines.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md closeout + W7 atomic commit</name>
  <files>.planning/STATE.md</files>
  <read_first>
    .planning/STATE.md (review most recent Phase-N closeout sections — Phase 10 closeout 2026-05-25 is the format template),
    .planning/phases/12-rename-to-mostlyright/README.md (the source of truth this closeout summarizes)
  </read_first>
  <action>
    Step 1 — append a Phase 12 closeout section to `.planning/STATE.md` IMMEDIATELY AFTER the Current Position block, matching the Phase 10 closeout format. Template:

    ```markdown
    ## Phase 12 closeout (2026-05-25) — Rename `tradewinds` → `mostlyright`

    Merge commit pending. Mechanical end-to-end rename of every in-repo identifier from `tradewinds` to `mostlyright`. Zero behavior change. Zero test regressions at every wave gate. The repo `helloiamvu/tradewinds` URL stays unchanged for now (operator may rename out-of-band).

    **Requirements shipped (10/10):**
    - RENAME-01: W1 atomic — 3 Python source dirs `git mv`'d; 3 PyPI distros renamed; 5 npm packages renamed; uv.lock + pnpm-lock.yaml regenerated.
    - RENAME-02: W2 — ~1074 Python imports rewritten in 3 batches (packages/, tests/, scripts/) with test-green gate after each. Schema `$id` URL `tradewinds.dev` → `mostlyright.dev` rewritten with lockstep test update.
    - RENAME-03: W3 — ~61 TS imports rewritten + `pnpm codegen` regen + idempotency verified.
    - RENAME-04: W4 — `TRADEWINDS_CACHE_DIR` → `MOSTLYRIGHT_CACHE_DIR` env var migration with `_cache_dir.py` back-compat shim emitting DeprecationWarning on legacy var. 3 new unit tests (canonical-wins, legacy-warns, default-fallback).
    - RENAME-05: W4 — default cache path `~/.tradewinds/cache/v1/` → `~/.mostlyright/cache/v1/`; `docs/cache-migration.md` documents user-side `mv ~/.tradewinds ~/.mostlyright`.
    - RENAME-06: W5 — ~85 docs/ + ~20 CLAUDE.md + root README mentions rewritten. `.planning/` archive (~4858 mentions) preserved.
    - RENAME-07: W6 — 10 CI workflow files + 1 path-filters JSON rewritten with FILENAMES PRESERVED (OIDC trusted-publisher bindings stay valid). `TRADEWINDS_TS_LIVE` → `MOSTLYRIGHT_TS_LIVE`. `TW_HOSTED_URL` + repo URL preserved.
    - RENAME-08: W7 parity gate — `uv run pytest -q` (incl. @pytest.mark.live) GREEN under `from mostlyright import research`. 5-fixture byte-equivalent. Full Python + TS suites GREEN.
    - RENAME-09: W7 operator pre-flight — Phase 12 README includes 4-line operator-confirmation checklist (OP1-OP4) for the PR description.
    - RENAME-10: W7 Phase 12 README documents rename history, legacy folder rename, orphaned PyPI/npm names, v0.3 back-compat removal timeline.

    **Preserve-list intact (these literals UNCHANGED — count vs pre-Phase-12 baseline):**
    - `MostlyRight*` deprecation aliases (CORE-04, scheduled v0.3 removal — separate decision)
    - `mostlyright_v1` parser_name enum (LINEAGE-01)
    - `monorepo-v0.14.1` lift-source citations
    - `mostlyright==0.14.1` parity citations (50+ docstrings)
    - `tradewinds-legacy` (operator's renamed folder reference)
    - `github.com/helloiamvu/tradewinds` repo URL (repo stays)
    - `TW_HOSTED_URL` env var (Phase 7 cryptic prefix, not brand)

    **Test coverage delta:**
    - Python: 1971 → 1974 (+3 new — test_cache_env_back_compat.py canonical-wins/legacy-warns/default-fallback)
    - TS: 1323 → 1323 (no new tests; rename mechanically updates existing test paths)
    - Parity gate green (5 fixtures byte-equivalent under new module name).

    **Files added:**
    - `packages/core/src/mostlyright/_internal/_cache_dir.py`
    - `packages/core/tests/test_cache_env_back_compat.py`
    - `scripts/_phase12_rename.py`, `scripts/_phase12_rename_ts.py`
    - `docs/cache-migration.md`
    - `.planning/phases/12-rename-to-mostlyright/{12-CONTEXT,12-RESEARCH,12-{01..07}-PLAN,12-{01..07}-SUMMARY,README}.md`

    **Operator-gated follow-ups (BLOCKED pending PR merge):**
    1. OP1 ~/Documents/GitHub/mostlyright → mostlyright-legacy (manual rename, prevents sys.path[0] collision).
    2. OP2 3 PyPI pending publishers (mostlyright, mostlyright-weather, mostlyright-markets) — pypi.org.
    3. OP3 @mostlyrightmd npm scope claim — npmjs.com.
    4. OP4 4 npm OIDC pending publishers (@mostlyrightmd/{core,weather,markets} + meta) — npmjs.com.
    5. Tag `v0.2.0` (or appropriate) → publish renamed PyPI distros.
    6. Tag `vts-0.2.0` (or appropriate) → publish renamed npm packages.
    7. v0.3: remove `TRADEWINDS_CACHE_DIR` back-compat branch.

    **Unblocks:** Phase 13+ on renamed identifiers.
    ```

    Also bump the STATE.md frontmatter:
      `status:` → "Phase 12 (Rename tradewinds → mostlyright) code-complete; operator pre-flight OP1-OP4 pending before PR merge."
      `stopped_at:` → "Phase 12 closeout; merged-vision → main PR queued (3-reviewer dispatch); operator OP1-OP4 required before merge"
      `last_updated:` → current ISO timestamp
      `last_activity:` → "2026-05-25 - Phase 12 mechanical rename `tradewinds` → `mostlyright` code-complete; zero test regressions at every wave gate"
      `progress.completed_phases:` increment by 1 (if STATE conventions count this phase)
      `progress.completed_plans:` increment appropriately (7 plans land in this phase)

    Step 2 — single W7 atomic commit:
      git add .planning/STATE.md .planning/phases/12-rename-to-mostlyright/README.md .planning/phases/12-rename-to-mostlyright/12-07-SUMMARY.md
      git commit -m "phase12 W7: parity-gate green + Phase 12 README + STATE.md closeout"

    Step 3 — FINAL summary output to operator:
      git log --oneline -7  # show all 7 wave commits in order
      Echo: "Phase 12 code-complete. Open PR `merged-vision` → `main` with the 4-line operator-confirmation checklist from .planning/phases/12-rename-to-mostlyright/README.md §Operator pre-flight in the PR description. Reviewers: codex `high` + python-architect + ts-architect (parallel; max 5 iterations)."
  </action>
  <verify>
    <automated>
    # 1. STATE.md closeout section added
    grep -E '^## Phase 12 closeout' .planning/STATE.md | wc -l  # expect 1
    # 2. STATE.md frontmatter updated
    grep -E '^status:' .planning/STATE.md | grep -ci 'Phase 12'  # expect 1
    # 3. All 7 wave commits in git history (post-pre-flight ordering)
    git log --oneline -7 | grep -cE 'phase12 W[1-7]' | awk '$1==7 {exit 0} {exit 1}'
    # 4. Phase 12 README + W7 SUMMARY linked
    test -f .planning/phases/12-rename-to-mostlyright/README.md
    test -f .planning/phases/12-rename-to-mostlyright/12-07-SUMMARY.md
    </automated>
  </verify>
  <done>
    STATE.md closeout section appended in standard Phase-N format. W7 atomic commit landed. 7 wave commits visible in git log. Phase 12 README and W7 SUMMARY committed. Phase 12 is code-complete; merged-vision → main PR is ready to open (operator-gated).
  </done>
</task>

</tasks>

<verification>
  <automated>
    # WAVE 7 ACCEPTANCE GATE — the final phase-level gate
    # 1. Parity gate (the HARD GATE per CLAUDE.md)
    uv run pytest tests/test_parity.py -q  # exit 0
    # 2. Full Python suite (incl. live)
    uv run pytest -q  # exit 0
    # 3. Full TS suite
    pnpm -r run typecheck  # exit 0
    CI=1 pnpm -r run test  # exit 0
    # 4. Smoke imports
    python -c "import mostlyright; from mostlyright import research"
    # 5. Wheels build under new names
    ls dist/mostlyright-*.whl dist/mostlyright_weather-*.whl dist/mostlyright_markets-*.whl > /dev/null
    # 6. FINAL verification grep (the most important assertion)
    grep -rEn 'tradewinds' packages/ packages-ts/ tests/ scripts/ docs/ README.md CLAUDE.md .github/workflows/ --include='*.py' --include='*.ts' --include='*.tsx' --include='*.json' --include='*.toml' --include='*.md' --include='*.yml' --include='*.yaml' | grep -v 'mostlyright_v1\|MostlyRight\|monorepo-v0\.14\.1\|mostlyright==0\.14\.1\|tradewinds-legacy\|github\.com/helloiamvu/tradewinds\|\.tradewinds\|TW_HOSTED_URL\|\.planning/' | wc -l  # expect 0
    # 7. Codegen idempotent
    pnpm codegen >/dev/null && git status --porcelain packages-ts/*/src/data/generated/ schemas/json/ | wc -l | grep -q '^0$'
    # 8. Phase 12 README + STATE.md closeout committed
    test -f .planning/phases/12-rename-to-mostlyright/README.md
    grep -q 'Phase 12 closeout' .planning/STATE.md
    # 9. 7 wave commits in git history
    git log --oneline -10 | grep -cE 'phase12 W[1-7]' | awk '$1==7 {exit 0} {exit 1}'
  </automated>
</verification>

<success_criteria>
- Parity gate `uv run pytest tests/test_parity.py -q` GREEN under `from mostlyright import research` (the HARD GATE per CLAUDE.md).
- Full Python suite + Live tests GREEN (`uv run pytest -q`).
- Full TS suite + typecheck GREEN.
- Smoke imports work for both Python and TS.
- 3 wheels build under new names.
- Final verification grep returns 0 unauthorized `tradewinds` refs.
- `pnpm codegen` idempotent.
- Phase 12 README written with operator OP1-OP4 checklist, post-merge follow-ups, v0.3 timeline.
- STATE.md closeout section appended in standard Phase-N format.
- 7 atomic wave commits visible in git log.
- W7 atomic commit landed.
</success_criteria>

<output>
After completion, create `.planning/phases/12-rename-to-mostlyright/12-07-SUMMARY.md` documenting:
- Parity gate result (5 fixtures pass/fail status)
- Full Python suite test counts (pre vs post)
- Full TS suite test counts (pre vs post)
- Smoke import results
- Wheel build product list
- Final verification grep result (line count)
- Codegen idempotency result
- 7 wave commit SHAs in order
- Operator OP1-OP4 checklist re-stated for the PR description
- Ready-to-open PR title: `phase12: rename tradewinds → mostlyright (zero behavior change)`
</output>
