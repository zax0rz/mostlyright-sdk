---
phase: 12-rename-to-mostlyright
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/pyproject.toml
  - packages/weather/pyproject.toml
  - packages/markets/pyproject.toml
  - pyproject.toml
  - packages/core/src/tradewinds/  # -> packages/core/src/mostlyright/ (git mv)
  - packages/weather/src/tradewinds/  # -> packages/weather/src/mostlyright/ (git mv)
  - packages/markets/src/tradewinds/  # -> packages/markets/src/mostlyright/ (git mv)
  - uv.lock
  - packages-ts/codegen/package.json
  - packages-ts/core/package.json
  - packages-ts/weather/package.json
  - packages-ts/markets/package.json
  - packages-ts/meta/package.json
  - .changeset/config.json
  - package.json
  - pnpm-lock.yaml
autonomous: true
requirements:
  - RENAME-01
tags:
  - rename
  - workspace
  - lockfile
must_haves:
  truths:
    - "`uv sync` succeeds with three workspace members named `mostlyright`, `mostlyright-weather`, `mostlyright-markets`"
    - "`uv build --all-packages` produces three wheels named `mostlyright-*.whl`, `mostlyright_weather-*.whl`, `mostlyright_markets-*.whl`"
    - "`pnpm install` succeeds with 5 packages renamed under `@mostlyright/*` scope (+ unscoped meta `mostlyright`)"
    - "`git log --follow packages/core/src/mostlyright/research.py` shows pre-rename history (blame preserved via `git mv`)"
    - "EXPECTED RED: `uv run pytest -m \"not live\" -q` is RED at end of W1 because import sites still say `tradewinds` — documented expected state, fixed in W2"
  artifacts:
    - path: "packages/core/src/mostlyright/__init__.py"
      provides: "Renamed Python namespace package root (formerly tradewinds)"
    - path: "packages/core/pyproject.toml"
      provides: "[project] name = mostlyright; [tool.hatch.build.targets.wheel] packages = [src/mostlyright]"
    - path: "uv.lock"
      provides: "Regenerated lockfile referencing mostlyright workspace members"
    - path: "pnpm-lock.yaml"
      provides: "Regenerated lockfile referencing @mostlyright/* packages"
  key_links:
    - from: "packages/weather/pyproject.toml dependencies"
      to: "mostlyright (core)"
      via: "inter-package version pin `mostlyright>=0.1.0rc1,<0.2`"
    - from: "packages-ts/meta/package.json dependencies"
      to: "@mostlyright/{core,weather,markets}"
      via: "workspace:* deps"
---

<objective>
W1 — Atomic rename of the 3 Python package directories + 3 PyPI distribution names + 5 npm package names + workspace lockfiles. Single atomic wave so the workspace is internally consistent (lockfiles match pyproject + package.json). The Python import statements + TS imports still say `tradewinds` post-W1 — that is the EXPECTED RED state, fixed in W2 + W3.

Purpose: Establish the new identity at the build-system layer (pyproject + package.json + lockfiles). Subsequent waves fix import-site references.

Output: Three Python wheels build under new names; pnpm install succeeds. Tests are RED — documented.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-rename-to-mostlyright/12-CONTEXT.md
@.planning/phases/12-rename-to-mostlyright/12-RESEARCH.md
@.planning/REVIEW-DISCIPLINE.md
@.planning/CROSS-SDK-SYNC.md
@CLAUDE.md
@packages/core/pyproject.toml
@packages/weather/pyproject.toml
@packages/markets/pyproject.toml
@pyproject.toml
@packages-ts/core/package.json
@packages-ts/meta/package.json
@.changeset/config.json

# Operator pre-flight confirmation REQUIRED BEFORE this wave ships:
#   OP1: `mv ~/Documents/GitHub/mostlyright ~/Documents/GitHub/mostlyright-legacy` (manual)
#   OP2: 3 PyPI pending publishers registered (mostlyright / mostlyright-weather / mostlyright-markets) - pypi.org
#   OP3: @mostlyright npm scope claimed - npmjs.com
#   OP4: 4 npm OIDC pending publishers registered (@mostlyright/{core,weather,markets} + unscoped meta) - npmjs.com
# If OP1-OP4 not confirmed, STOP and prompt operator. The plan task list assumes they are done.

# PRESERVE-LIST (DO NOT MODIFY these literals anywhere in W1):
#   - MostlyRight (deprecation aliases)
#   - mostlyright_v1 (LINEAGE-01 parser_name enum)
#   - monorepo-v0.14.1 (lift-source citations)
#   - mostlyright==0.14.1 (parity citations)
#   - tradewinds-legacy (operator's renamed folder)
#   - TW_HOSTED_URL (Phase 7 env var; "TW" prefix NOT tradewinds — preserved)
#   - github.com/helloiamvu/tradewinds (repo URL stays; package.json repository.url + pyproject.toml [project.urls] STAY pointing here)
</context>

<tasks>

<task type="auto">
  <name>Task 1: git mv Python source directories + rewrite all pyproject.toml files</name>
  <files>
    packages/core/src/tradewinds/ -> packages/core/src/mostlyright/ (git mv),
    packages/weather/src/tradewinds/ -> packages/weather/src/mostlyright/ (git mv),
    packages/markets/src/tradewinds/ -> packages/markets/src/mostlyright/ (git mv),
    packages/core/pyproject.toml,
    packages/weather/pyproject.toml,
    packages/markets/pyproject.toml,
    pyproject.toml
  </files>
  <read_first>
    .planning/phases/12-rename-to-mostlyright/12-CONTEXT.md (decisions §Rename safety rules — git mv mandatory, no git rm+add),
    .planning/phases/12-rename-to-mostlyright/12-RESEARCH.md (§Pitfall 3 PEP 420 namespace root must NOT carry __init__.py in weather/markets; §Pitfall 4 hatchling wheel target; §Pitfall 5 inter-package pins),
    packages/core/pyproject.toml,
    packages/weather/pyproject.toml,
    packages/markets/pyproject.toml,
    pyproject.toml (workspace root)
  </read_first>
  <action>
    Step 1 — git mv 3 source directories (preserves git log --follow):
      git mv packages/core/src/tradewinds packages/core/src/mostlyright
      git mv packages/weather/src/tradewinds packages/weather/src/mostlyright
      git mv packages/markets/src/tradewinds packages/markets/src/mostlyright
    Confirm: `find packages/weather/src/mostlyright packages/markets/src/mostlyright -maxdepth 1 -name '__init__.py'` returns 0 lines (PEP 420 namespace root must NOT have __init__.py in these sibling distros; only packages/core/src/mostlyright/__init__.py exists at the namespace root).

    Step 2 — rewrite each `packages/{core,weather,markets}/pyproject.toml` with these 4 exact field changes:
      [project] name = "tradewinds" -> "mostlyright"  (core)
      [project] name = "tradewinds-weather" -> "mostlyright-weather"  (weather)
      [project] name = "tradewinds-markets" -> "mostlyright-markets"  (markets)
      [tool.hatch.build.targets.wheel] packages = ["src/tradewinds"] -> ["src/mostlyright"]  (all 3)
      [project] dependencies inter-package pin: every `"tradewinds>=0.1.0rc1,<0.2"` -> `"mostlyright>=0.1.0rc1,<0.2"`; same for `tradewinds-weather`/`tradewinds-markets` in optional-dependencies blocks
      Any `[tool.uv.sources]` keys naming `tradewinds*` -> `mostlyright*`
    PRESERVE: any comment line mentioning `monorepo-v0.14.1`, `mostlyright==0.14.1`, `mostlyright_v1` (these are citations, not the rename target).

    Step 3 — rewrite workspace root `pyproject.toml`:
      [project] dependencies = ["tradewinds", "tradewinds-weather", "tradewinds-markets"] -> ["mostlyright", "mostlyright-weather", "mostlyright-markets"]
      [tool.uv.sources] keys: tradewinds = { workspace = true } -> mostlyright = ...; same for weather + markets
      Any `[tool.coverage.run] source` or similar paths referencing `tradewinds` -> `mostlyright`

    Per D-CONTEXT.md decision: Python directories first (bigger blast radius — gives W2 something to import from).

    DO NOT rewrite any *.py source files in this task — that is W2's job. We are only renaming the directory and the toml fields here.
  </action>
  <verify>
    <automated>
    # 1. Directory rename completed cleanly (git treats as rename)
    git status --short | grep -E '^R  packages/(core|weather|markets)/src/' | wc -l  # expect 3 (or more if files renamed inside)
    # 2. pyproject.toml field rewrites complete — zero `tradewinds` literals remain in pyproject files (excluding comments referencing mostlyright/monorepo-v0.14.1)
    grep -n 'tradewinds' packages/core/pyproject.toml packages/weather/pyproject.toml packages/markets/pyproject.toml pyproject.toml | grep -v 'mostlyright\|monorepo-v0.14.1' | wc -l  # expect 0
    # 3. PEP 420 namespace cleanliness (weather/markets must NOT have __init__.py at namespace root)
    find packages/weather/src/mostlyright packages/markets/src/mostlyright -maxdepth 1 -name '__init__.py' | wc -l  # expect 0
    test -f packages/core/src/mostlyright/__init__.py  # expect exit 0
    </automated>
  </verify>
  <done>
    All 3 pyproject.toml files name = "mostlyright*"; all 3 wheel-target lines say "src/mostlyright"; workspace root references "mostlyright" workspace members. PEP 420 namespace clean. Git tracks directory rename (preserves blame).
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename 5 npm packages + .changeset/config.json + root package.json workspace deps</name>
  <files>
    packages-ts/codegen/package.json,
    packages-ts/core/package.json,
    packages-ts/weather/package.json,
    packages-ts/markets/package.json,
    packages-ts/meta/package.json,
    .changeset/config.json,
    package.json (root size-limit entries if they name @tradewinds/*)
  </files>
  <read_first>
    .planning/phases/12-rename-to-mostlyright/12-CONTEXT.md (npm rename scope),
    .planning/phases/12-rename-to-mostlyright/12-RESEARCH.md (§W1 Atomic npm package rename code example),
    packages-ts/codegen/package.json,
    packages-ts/core/package.json,
    packages-ts/weather/package.json,
    packages-ts/markets/package.json,
    packages-ts/meta/package.json,
    .changeset/config.json,
    package.json
  </read_first>
  <action>
    Step 1 — rewrite 5 package.json `name` fields (exact substitutions):
      packages-ts/codegen/package.json: "name": "@tradewinds/codegen" -> "@mostlyright/codegen"
      packages-ts/core/package.json:    "name": "@tradewinds/core"    -> "@mostlyright/core"
      packages-ts/weather/package.json: "name": "@tradewinds/weather" -> "@mostlyright/weather"
      packages-ts/markets/package.json: "name": "@tradewinds/markets" -> "@mostlyright/markets"
      packages-ts/meta/package.json:    "name": "tradewinds"          -> "mostlyright"  (unscoped meta)

    Step 2 — rewrite workspace deps in EACH affected package.json (pnpm resolves workspace:* by package name, not path):
      packages-ts/meta/package.json `dependencies`/`devDependencies`/`peerDependencies`:
        "@tradewinds/core": "workspace:*"    -> "@mostlyright/core": "workspace:*"
        "@tradewinds/weather": "workspace:*" -> "@mostlyright/weather": "workspace:*"
        "@tradewinds/markets": "workspace:*" -> "@mostlyright/markets": "workspace:*"
      packages-ts/weather/package.json `peerDependencies` + `devDependencies`:
        "@tradewinds/core" -> "@mostlyright/core"
      packages-ts/markets/package.json `peerDependencies` + `devDependencies`:
        "@tradewinds/core" -> "@mostlyright/core"
      packages-ts/codegen/package.json — if it references @tradewinds/* anywhere, rewrite to @mostlyright/*

    Step 3 — rewrite .changeset/config.json `fixed` group:
      "fixed": [["@tradewinds/core", "@tradewinds/weather", "@tradewinds/markets", "tradewinds"]]
        -> [["@mostlyright/core", "@mostlyright/weather", "@mostlyright/markets", "mostlyright"]]

    Step 4 — rewrite root package.json `size-limit` entries (or any other root-level config) that name @tradewinds/* — substitute to @mostlyright/*.

    PRESERVE: any string in package.json `repository.url` field that points to `github.com/helloiamvu/tradewinds` — repo URL stays unchanged per CONTEXT.md decision. Also preserve `description` / `keywords` if they mention "tradewinds" historically — those are W5's prose rewrite job, not W1's.

    Per D-CONTEXT.md: 1 commit per atomic wave (planner discretion — choosing 1 commit for the whole wave for atomicity).
  </action>
  <verify>
    <automated>
    # 1. All 5 package.json `name` fields renamed
    grep -h '"name"' packages-ts/codegen/package.json packages-ts/core/package.json packages-ts/weather/package.json packages-ts/markets/package.json packages-ts/meta/package.json | grep -E '"@mostlyright/(codegen|core|weather|markets)"|"name": "mostlyright"' | wc -l  # expect 5
    # 2. No remaining @tradewinds/ refs in non-repository.url fields
    grep -n '@tradewinds/' packages-ts/*/package.json .changeset/config.json | grep -v 'repository' | wc -l  # expect 0
    # 3. Meta unscoped renamed
    grep -n '"name": "tradewinds"' packages-ts/meta/package.json | wc -l  # expect 0
    grep -n '"name": "mostlyright"' packages-ts/meta/package.json | wc -l  # expect 1
    # 4. Changeset fixed group renamed
    grep -n '@mostlyright/core' .changeset/config.json | wc -l  # expect >=1
    </automated>
  </verify>
  <done>
    5 package.json files renamed; all workspace deps point to @mostlyright/*; Changesets fixed group references new names. repository.url fields PRESERVED at helloiamvu/tradewinds.
  </done>
</task>

<task type="auto">
  <name>Task 3: Regenerate uv.lock + pnpm-lock.yaml + verify wheel build + WAVE 1 GATE</name>
  <files>uv.lock, pnpm-lock.yaml</files>
  <read_first>
    .planning/phases/12-rename-to-mostlyright/12-RESEARCH.md (§Anti-Patterns — never hand-edit lockfiles; §Pitfall 3 PEP 420 wheel-layout check),
    packages/core/pyproject.toml (verify name="mostlyright"),
    packages-ts/meta/package.json
  </read_first>
  <action>
    Step 1 — regenerate uv.lock (do NOT use `uv lock --upgrade-package tradewinds` — the project name moved):
      rm uv.lock
      uv lock
    Step 2 — verify Python wheel build cleanly under new names:
      uv build --all-packages
    Inspect dist/ — expect three wheels: mostlyright-*.whl, mostlyright_weather-*.whl, mostlyright_markets-*.whl. Each wheel's METADATA `Requires-Dist` for weather/markets must say `mostlyright>=0.1.0rc1,<0.2` (NOT `tradewinds>=...`).
    Step 3 — regenerate pnpm-lock.yaml:
      pnpm install
    Step 4 — WAVE 1 EXPECTED-RED documentation:
      Running `uv run pytest -m "not live" -q` at this point IS EXPECTED TO BE RED — the import statements in *.py files still say `from tradewinds...`. This is documented per CONTEXT.md decision (W2 fixes it). Do NOT attempt to fix the test failures in W1 — escalate to W2.
    Step 5 — single atomic commit covering all 3 tasks of W1:
      git add packages/core/pyproject.toml packages/weather/pyproject.toml packages/markets/pyproject.toml pyproject.toml uv.lock packages-ts/*/package.json package.json .changeset/config.json pnpm-lock.yaml
      git add packages/core/src/mostlyright/ packages/weather/src/mostlyright/ packages/markets/src/mostlyright/  # the renamed directories
      git commit -m "phase12 W1: rename packages (pyproject + npm scope + dirs)"
      (No --no-verify per CLAUDE.md — if pre-commit hooks complain about a YAML/TOML/whitespace issue, fix it.)
  </action>
  <verify>
    <automated>
    # 1. uv.lock regenerated and references mostlyright (not tradewinds) as workspace project name
    grep -c 'name = "mostlyright"' uv.lock  # expect >=1
    grep -c 'name = "tradewinds"' uv.lock  # expect 0
    # 2. uv build produces 3 wheels with new names
    uv build --all-packages 2>&1 | tail -5
    ls dist/mostlyright-*.whl dist/mostlyright_weather-*.whl dist/mostlyright_markets-*.whl  # all 3 must exist
    # 3. Wheel METADATA Requires-Dist updated (Pitfall 5)
    unzip -p dist/mostlyright_weather-*.whl '*/METADATA' | grep -E '^Requires-Dist: mostlyright' | wc -l  # expect >=1
    unzip -p dist/mostlyright_weather-*.whl '*/METADATA' | grep -E '^Requires-Dist: tradewinds' | wc -l  # expect 0
    # 4. pnpm install succeeds
    pnpm install 2>&1 | tail -3
    test -f pnpm-lock.yaml
    # 5. pnpm-lock references @mostlyright (workspace project keys)
    grep -c '@mostlyright/' pnpm-lock.yaml  # expect >=4 (codegen, core, weather, markets references)
    # 6. PEP 420 wheel-layout test still green (the existing test_wheel_layout.py may rely on package path constants — DEFER FAILURE TO W2 if it depends on import strings; only enforce wheel-bytes-cleanliness here)
    unzip -l dist/mostlyright_weather-*.whl | grep -E 'mostlyright/__init__\.py' | wc -l  # expect 0 (PEP 420 — no init at namespace root in weather wheel)
    unzip -l dist/mostlyright_markets-*.whl | grep -E 'mostlyright/__init__\.py' | wc -l  # expect 0 (same for markets)
    unzip -l dist/mostlyright-*.whl | grep -E 'mostlyright/__init__\.py' | wc -l  # expect 1 (core OWNS the namespace root __init__.py)
    # 7. Commit landed
    git log --oneline -1 | grep -E 'phase12 W1'
    </automated>
  </verify>
  <done>
    Three Python wheels build cleanly under new names with correct Requires-Dist pins; pnpm install succeeds; uv.lock + pnpm-lock.yaml regenerated; W1 atomic commit landed. Tests are EXPECTED RED at this point — W2 starts here.
  </done>
</task>

</tasks>

<verification>
  <automated>
    # W1 acceptance gate — ALL must pass before W2 starts
    grep -c 'name = "mostlyright"' uv.lock | awk '$1 >= 1 {exit 0} {exit 1}'  # uv.lock has new project name
    grep -c 'tradewinds' packages/core/pyproject.toml packages/weather/pyproject.toml packages/markets/pyproject.toml pyproject.toml | grep -v ':0' | awk -F: 'NR{sum+=$2} END{exit sum>0}'  # all pyproject.toml files clean
    ls dist/mostlyright-*.whl dist/mostlyright_weather-*.whl dist/mostlyright_markets-*.whl > /dev/null  # 3 wheels exist
    grep -c '@tradewinds/' packages-ts/codegen/package.json packages-ts/core/package.json packages-ts/weather/package.json packages-ts/markets/package.json packages-ts/meta/package.json .changeset/config.json | awk -F: 'NR{sum+=$2} END{exit sum>0}'  # no tradewinds npm refs remain
    git log --oneline -1 | grep -q 'phase12 W1'  # W1 commit landed
  </automated>
</verification>

<success_criteria>
- `uv build --all-packages` produces 3 wheels: `mostlyright-*.whl`, `mostlyright_weather-*.whl`, `mostlyright_markets-*.whl`.
- Wheel METADATA `Requires-Dist` for weather/markets says `mostlyright>=0.1.0rc1,<0.2`.
- `pnpm install` succeeds with 5 packages renamed under `@mostlyright/*` scope + unscoped meta `mostlyright`.
- PEP 420 namespace clean: only `packages/core/src/mostlyright/__init__.py` exists at the namespace root.
- `git log --follow packages/core/src/mostlyright/research.py` shows pre-rename history (proof `git mv` preserved blame).
- Single atomic commit `phase12 W1: ...` landed via pre-commit hooks (no `--no-verify`).
- Tests RED — DOCUMENTED EXPECTED STATE; W2 fixes.
</success_criteria>

<output>
After completion, create `.planning/phases/12-rename-to-mostlyright/12-01-SUMMARY.md` documenting:
- 3 directory renames + lockfile regen results
- 3 wheel filenames produced + Requires-Dist samples
- Expected-RED test state (no fix attempted)
- Files modified
- Git commit SHA
</output>
