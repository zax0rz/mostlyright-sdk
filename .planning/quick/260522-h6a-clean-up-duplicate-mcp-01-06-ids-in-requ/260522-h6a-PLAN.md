---
quick_id: 260522-h6a
type: execute
mode: quick
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: []  # Hygiene-only docs cleanup; does not implement any requirement. Resolves MCP-XX ID collision per Phase 5 PLAN-00 §id_collision option (b).
must_haves:
  truths:
    - "The OLD narrow MCP-01..MCP-06 sub-section (header `### MCP Server (v0.2 milestone)` + its 6 bullet entries) is deleted from `.planning/REQUIREMENTS.md` — currently at ~lines 101-108."
    - "The ID-collision note paragraph at ~line 236 (`> **ID-collision note:** ...`) is deleted — it is resolved once the legacy entries are gone."
    - "The footer `Last updated` line is updated to reflect the cleanup: `*Last updated: 2026-05-22 — resolved MCP-01..06 ID collision; Phase 5 entries are canonical*` (or near-equivalent wording)."
    - "The parent `## v2 Requirements (Deferred)` heading and its intro line are PRESERVED — other sub-sections (Pandas 3.0 Migration, Cross-Source Diff Job, Markets API Client, Preprocessing) still belong under it."
    - "The canonical Phase 5 block (`## Phase 5: MCP Data Platform (v0.2+)` and its MCP-01..MCP-10 bullets at ~lines 232+) is UNCHANGED."
    - "The Phase 5 Traceability table (MCP-01..MCP-10 → Phase 5 → Pending) at ~lines 251-262 is UNCHANGED."
    - "Post-edit verification: `grep -nE '\\*\\*MCP-(0[1-9]|10)\\*\\*' .planning/REQUIREMENTS.md` shows exactly 10 bolded MCP-XX definitions (one per ID, all from the canonical Phase 5 block)."
    - "Post-edit verification: `grep -nE 'catalog_search|pull_pairs|validate_dataframe' .planning/REQUIREMENTS.md` returns no matches (old narrow tool-name strings are gone)."
    - "Pre-commit hooks pass cleanly (`uv run pre-commit run --files .planning/REQUIREMENTS.md`); no `--no-verify` used."
  artifacts:
    - path: .planning/REQUIREMENTS.md
      provides: "Cleaned requirements file with single canonical MCP-01..MCP-10 set (Phase 5) and no duplicate MCP-XX IDs"
      contains: "## Phase 5: MCP Data Platform"
  key_links:
    - from: .planning/REQUIREMENTS.md (canonical MCP-01..MCP-10 block, ~lines 238-247)
      to: .planning/REQUIREMENTS.md (Phase 5 Traceability table, ~lines 251-262)
      via: "1:1 correspondence between bold definitions and traceability rows — preserved after cleanup"
      pattern: "^- \\[ \\] \\*\\*MCP-(0[1-9]|10)\\*\\*"
---

<objective>
Resolve the MCP-XX ID collision in `.planning/REQUIREMENTS.md` per Phase 5 PLAN-00's locked decision (option (b): DELETE the legacy narrow MCP-01..06 entries; Phase 5 MCP-01..10 is canonical).

Purpose: REQUIREMENTS.md currently defines `MCP-01..MCP-06` twice — once as the OLD narrow tool surface (`tradewinds-mcp-server` console script, `catalog_search`, `pull_pairs`, `validate_dataframe`, JSON-RPC tests, TOON serialization) under `## v2 Requirements (Deferred)`, and once as the broader Phase 5 vision (`list_sources`, `describe_source`, `ingest`, `query`, `get_schema`, etc.). Phase 5 PLAN-00 locked option (b): delete the legacy entries.

Output: REQUIREMENTS.md with exactly one definition per MCP-01..MCP-10 (all from the canonical Phase 5 block), no duplicates, no stale collision note, footer updated.

Scope discipline: this is a docs-only edit. No code, no tests, no schema changes. The Phase 5 PLAN-00 ALSO covers this cleanup as a Wave-0 prerequisite — but this quick task lets us resolve the hygiene issue now (independently of Phase 5 plan execution) so any reader of REQUIREMENTS.md sees an unambiguous canonical set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phase-05-mcp-data-platform/PLAN-00-requirements-id-cleanup.md
@./CLAUDE.md

<edit_targets>
The three precise edits (re-read REQUIREMENTS.md fresh before editing; line numbers below are approximate and may shift):

**Edit 1 — DELETE the OLD narrow block** (~lines 101-108):

```markdown
### MCP Server (v0.2 milestone)

- **MCP-01**: `tradewinds-mcp-server` console script registers with Claude Code via `mcp` Python SDK (FastMCP pattern)
- **MCP-02**: `catalog_search(market, contract_type) → JSON` tool — returns source matrix with validated/known-bad source-tuple combinations
- **MCP-03**: `pull_pairs(contract, sources, from_date, to_date, format="toon") → bytes|str` tool — wraps `research()` Mode 2, registers schema server-side, returns `schema_id` for later validation
- **MCP-04**: `validate_dataframe(data, schema_id, format, allow_source_drift=None) → JSON` tool — runs Validator, returns violations + quarantine summary + source-mismatch flags
- **MCP-05**: JSON-RPC subprocess integration tests for all 3 tools
- **MCP-06**: TOON serialization at MCP tool boundary (no raw `pd.DataFrame` returns)
```

Delete the `### MCP Server (v0.2 milestone)` heading AND all 6 bullets. The blank line separator before the next sub-section (`### Pandas 3.0 Migration (v0.2)`) should remain so the section spacing is consistent. The parent `## v2 Requirements (Deferred)` heading + its intro line ("Acknowledged but not in v0.1.0 roadmap.") MUST be preserved.

**Edit 2 — DELETE the ID-collision note** (~line 236):

```markdown
> **ID-collision note:** The earlier `## v2 Requirements (Deferred) > ### MCP Server (v0.2 milestone)` section uses the identifiers `MCP-01..MCP-06` for narrow MCP tool surface (`tradewinds-mcp-server` console script, `catalog_search`, `pull_pairs`, `validate_dataframe`, JSON-RPC tests, TOON serialization). Those entries are SUPERSEDED by the broader Phase 5 vision below — same identifiers, expanded scope. Resolve before Phase 5 PLAN.md is written: either (a) renumber the old entries (e.g. `MCP-LEGACY-01..06`), (b) delete the old entries and treat Phase 5 as canonical, or (c) renumber Phase 5 (e.g. `PLAT-01..10`). Default recommendation: option (b) — the Phase 5 vision subsumes the old narrow MCP tools list.
```

Delete the entire blockquote. The collision is resolved once Edit 1 is applied, so no replacement marker is needed; the footer update (Edit 3) preserves the audit trail.

**Edit 3 — UPDATE the footer `Last updated` line** (~line 268):

Current:
```markdown
*Last updated: 2026-05-22 — added Phase 5 (MCP Data Platform, MCP-01..MCP-10) post-v0.1.0 vision*
```

Replace with:
```markdown
*Last updated: 2026-05-22 — resolved MCP-01..06 ID collision; Phase 5 entries are canonical*
```
</edit_targets>

<must_preserve>
- `## v2 Requirements (Deferred)` heading + its intro line ("Acknowledged but not in v0.1.0 roadmap.")
- `### Pandas 3.0 Migration (v0.2)` sub-section (PANDAS3-01..02)
- `### Cross-Source Diff Job (v0.1.1)` sub-section (DIFF-01..03)
- `### Markets API Client (Sprint 0.5+)` sub-section (KALSHI-01, POLY-01)
- `### Preprocessing (Sprint 0.5+)` sub-section (PREP-01..02)
- `## Phase 5: MCP Data Platform (v0.2+)` heading + intro + MCP-01..MCP-10 bullets (canonical)
- `### Phase 5 Traceability` table (MCP-01..MCP-10 → Phase 5 → Pending)
- The v1 Requirements section in its entirety (PARITY/CORE/CATALOG/MARKETS/RESEARCH/CACHE/PERF/PKG/DOCS/MIGRATION/CI blocks)
- The v1 Traceability table at ~lines 163-218 and Coverage block at ~lines 220-230
- The v3+ Requirements section and Out of Scope table
</must_preserve>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply three edits to REQUIREMENTS.md + run grep verification + commit</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Step 1 — Branch hygiene (per CLAUDE.md "never commit directly to main"):

Confirm current branch is NOT `main` and NOT `merged-vision` (this is a worktree on `claude/laughing-galileo-52af15` per the recent commits, which is fine). If somehow on `main`, create a quick branch first: `git switch -c quick/260522-h6a-mcp-id-cleanup`.

Step 2 — Re-read `.planning/REQUIREMENTS.md` fully before editing to confirm current line numbers. Line numbers in `<edit_targets>` may have shifted; the textual anchors (header text, bullet IDs, footer string) are the source of truth.

Step 3 — Apply Edit 1: Use the `Edit` tool with the old-string being the full block to delete (the `### MCP Server (v0.2 milestone)` heading plus its blank line plus all six bullets MCP-01..MCP-06), and the new-string being empty (or a single blank line preserving spacing — match the surrounding section's blank-line convention). Verify the next sub-section (`### Pandas 3.0 Migration (v0.2)`) is still preceded by exactly one blank line after the edit.

Step 4 — Apply Edit 2: Use the `Edit` tool to delete the ID-collision note blockquote. Old-string = the full `> **ID-collision note:** ...` line (it is a single long line in the file). New-string = empty. Preserve the blank line separator that surrounds the blockquote so the `## Phase 5: MCP Data Platform (v0.2+)` heading and the first canonical MCP-01 bullet remain visually separated.

Step 5 — Apply Edit 3: Use the `Edit` tool to replace the footer `*Last updated: 2026-05-22 — added Phase 5 (MCP Data Platform, MCP-01..MCP-10) post-v0.1.0 vision*` with `*Last updated: 2026-05-22 — resolved MCP-01..06 ID collision; Phase 5 entries are canonical*`.

Step 6 — Run grep verification suite. Each command's expected outcome is in the `<verify>` block below; run them in this order and confirm all pass:

```bash
# (a) OLD heading is gone — must NOT match
grep -n "### MCP Server (v0.2 milestone)" .planning/REQUIREMENTS.md
# Expected: no output, exit 1

# (b) Old narrow tool names are gone — must NOT match
grep -nE "catalog_search|pull_pairs|validate_dataframe" .planning/REQUIREMENTS.md
# Expected: no output, exit 1

# (c) ID-collision note language is gone
grep -n "ID-collision note" .planning/REQUIREMENTS.md
# Expected: no output, exit 1
grep -n "MCP-LEGACY" .planning/REQUIREMENTS.md
# Expected: no output, exit 1 (option (b) was DELETE, not renumber)

# (d) Exactly 10 bold MCP-XX definitions remain (MCP-01..MCP-10, canonical Phase 5 block only)
grep -cE "\*\*MCP-(0[1-9]|10)\*\*" .planning/REQUIREMENTS.md
# Expected: 10

# (e) Each MCP-XX appears exactly twice overall: once as a bold definition in the Phase 5 block, once as a row in the Phase 5 Traceability table
for n in 01 02 03 04 05 06 07 08 09 10; do
  echo "MCP-$n: $(grep -c "MCP-$n" .planning/REQUIREMENTS.md)"
done
# Expected: each line ends in "2"

# (f) v2 Requirements sub-sections that should remain are intact
grep -n "^### Pandas 3.0 Migration" .planning/REQUIREMENTS.md   # Expected: 1 match
grep -n "^### Cross-Source Diff Job" .planning/REQUIREMENTS.md  # Expected: 1 match
grep -n "^### Markets API Client" .planning/REQUIREMENTS.md     # Expected: 1 match
grep -n "^### Preprocessing" .planning/REQUIREMENTS.md          # Expected: 1 match

# (g) Footer was updated
grep -n "resolved MCP-01..06 ID collision" .planning/REQUIREMENTS.md
# Expected: 1 match (in footer)
grep -n "added Phase 5 (MCP Data Platform, MCP-01..MCP-10) post-v0.1.0 vision" .planning/REQUIREMENTS.md
# Expected: no output, exit 1 (old footer text replaced)
```

If ANY check fails, fix the edit and re-run the full suite. Do not proceed until all checks pass.

Step 7 — Run pre-commit on the modified file (per CLAUDE.md "Pre-commit + pre-push hooks mandatory. No --no-verify."):

```bash
uv run pre-commit run --files .planning/REQUIREMENTS.md
```

Expected: exit 0. If the hook auto-fixes anything (whitespace, etc.), re-stage and re-run.

Step 8 — Commit. Use a single commit with a message that references the Phase 5 PLAN-00 locked decision so future readers can find the rationale:

```bash
git add .planning/REQUIREMENTS.md
git commit -m "$(cat <<'EOF'
docs(requirements): delete legacy MCP-01..06; Phase 5 MCP-01..10 canonical

Resolves the MCP-XX ID collision in REQUIREMENTS.md per Phase 5 PLAN-00
locked decision (option (b): DELETE the legacy narrow entries).

- Delete `### MCP Server (v0.2 milestone)` sub-section under `## v2 Requirements (Deferred)` (legacy narrow tool surface: catalog_search, pull_pairs, validate_dataframe, JSON-RPC tests, TOON boundary).
- Delete the ID-collision note paragraph from the Phase 5 section (resolved by the deletion above).
- Update footer `Last updated` line to record the cleanup.

The canonical Phase 5 MCP-01..MCP-10 block (list_sources, describe_source,
ingest, query, get_schema, temporal-safety, etc.) and its Traceability table
are unchanged. After this commit, each MCP-XX has exactly one definition.

Quick task: 260522-h6a
Refs: .planning/phase-05-mcp-data-platform/PLAN-00-requirements-id-cleanup.md
EOF
)"
```

Step 9 — Final sanity check: `git log -1 --stat` should show ONLY `.planning/REQUIREMENTS.md` modified; the file should have a net deletion of ~9 lines (6 bullets + 1 heading + 1 long collision note line + footer rewrite is roughly net-equal). If unrelated files are staged, abort and fix.
  </action>
  <verify>
    <automated>set -e; ! grep -q "### MCP Server (v0.2 milestone)" .planning/REQUIREMENTS.md && ! grep -qE "catalog_search|pull_pairs|validate_dataframe" .planning/REQUIREMENTS.md && ! grep -q "ID-collision note" .planning/REQUIREMENTS.md && ! grep -q "MCP-LEGACY" .planning/REQUIREMENTS.md && [ "$(grep -cE '\*\*MCP-(0[1-9]|10)\*\*' .planning/REQUIREMENTS.md)" = "10" ] && grep -q "resolved MCP-01..06 ID collision" .planning/REQUIREMENTS.md && grep -q "^### Pandas 3.0 Migration" .planning/REQUIREMENTS.md && grep -q "^### Cross-Source Diff Job" .planning/REQUIREMENTS.md && grep -q "^### Markets API Client" .planning/REQUIREMENTS.md && grep -q "^### Preprocessing" .planning/REQUIREMENTS.md && grep -q "## Phase 5: MCP Data Platform" .planning/REQUIREMENTS.md && uv run pre-commit run --files .planning/REQUIREMENTS.md && echo "ALL CHECKS PASS"</automated>
  </verify>
  <done>
- All 9 grep checks in Step 6 pass with their expected outcomes.
- `uv run pre-commit run --files .planning/REQUIREMENTS.md` exits 0.
- A single commit is on the current branch modifying only `.planning/REQUIREMENTS.md`, with a message referencing Phase 5 PLAN-00 option (b).
- No `--no-verify` was used.
- The canonical Phase 5 MCP-01..MCP-10 block, the Phase 5 Traceability table, the v1 Requirements section, and the remaining v2 Requirements sub-sections (Pandas 3.0 Migration, Cross-Source Diff Job, Markets API Client, Preprocessing) are all intact.
  </done>
</task>

</tasks>

<verification>
## Plan-Level Checks

| # | Check | Command | Expected |
|---|-------|---------|----------|
| 1 | OLD narrow heading deleted | `grep -n "### MCP Server (v0.2 milestone)" .planning/REQUIREMENTS.md` | no output, exit 1 |
| 2 | OLD narrow tool names gone | `grep -nE "catalog_search\|pull_pairs\|validate_dataframe" .planning/REQUIREMENTS.md` | no output, exit 1 |
| 3 | ID-collision note removed | `grep -n "ID-collision note" .planning/REQUIREMENTS.md` | no output, exit 1 |
| 4 | No MCP-LEGACY renumbering happened | `grep -c "MCP-LEGACY" .planning/REQUIREMENTS.md` | 0 |
| 5 | Exactly 10 bold canonical MCP-XX defs | `grep -cE "\*\*MCP-(0[1-9]\|10)\*\*" .planning/REQUIREMENTS.md` | 10 |
| 6 | Each MCP-XX appears exactly twice (def + traceability row) | loop in Task 1 Step 6(e) | every line ends in "2" |
| 7 | Footer updated | `grep -n "resolved MCP-01..06 ID collision" .planning/REQUIREMENTS.md` | 1 match |
| 8 | Old footer string gone | `grep -n "added Phase 5 (MCP Data Platform, MCP-01..MCP-10) post-v0.1.0 vision" .planning/REQUIREMENTS.md` | no output, exit 1 |
| 9 | v2 sub-sections preserved | `grep -nE "^### (Pandas 3.0 Migration\|Cross-Source Diff Job\|Markets API Client\|Preprocessing)" .planning/REQUIREMENTS.md` | 4 matches |
| 10 | Phase 5 heading + traceability preserved | `grep -nE "^## Phase 5: MCP Data Platform\|^### Phase 5 Traceability" .planning/REQUIREMENTS.md` | 2 matches |
| 11 | Pre-commit green | `uv run pre-commit run --files .planning/REQUIREMENTS.md` | exit 0 |
| 12 | One commit modifying only REQUIREMENTS.md | `git log -1 --stat` | exactly 1 file changed: `.planning/REQUIREMENTS.md` |
</verification>

<success_criteria>
- [ ] The OLD narrow `### MCP Server (v0.2 milestone)` sub-section (heading + 6 bullets MCP-01..MCP-06) is deleted from `.planning/REQUIREMENTS.md`.
- [ ] The ID-collision note paragraph in the Phase 5 section is deleted (resolved by the deletion above).
- [ ] The footer `Last updated` line is rewritten to reflect the cleanup (`resolved MCP-01..06 ID collision; Phase 5 entries are canonical`).
- [ ] The canonical Phase 5 MCP-01..MCP-10 block (including its Traceability table) is unchanged.
- [ ] The parent `## v2 Requirements (Deferred)` heading + intro + remaining sub-sections (Pandas 3.0 Migration, Cross-Source Diff Job, Markets API Client, Preprocessing) are preserved.
- [ ] All 12 verification checks pass.
- [ ] Pre-commit hooks pass without `--no-verify`.
- [ ] One commit on the working branch modifies only `.planning/REQUIREMENTS.md`, with a message referencing Phase 5 PLAN-00 option (b).
</success_criteria>

<output>
After completion, append a one-line entry to STATE.md's "Quick Tasks Completed" table:

```
| 260522-h6a | Clean up duplicate MCP-01..06 IDs in REQUIREMENTS.md (Phase 5 PLAN-00 option (b)) | 2026-05-22 | <commit-sha> | [260522-h6a-clean-up-duplicate-mcp-01-06-ids-in-requ](./quick/260522-h6a-clean-up-duplicate-mcp-01-06-ids-in-requ/) |
```

No SUMMARY.md is required for a quick task this small — the commit message + STATE.md row capture the relevant audit trail. If executor judgment says a brief SUMMARY is warranted (e.g., reviewer-requested), write it to `.planning/quick/260522-h6a-clean-up-duplicate-mcp-01-06-ids-in-requ/260522-h6a-SUMMARY.md` per the standard template.
</output>
