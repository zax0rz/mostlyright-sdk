---
quick_id: 260524-qdu
plan: 01
description: Add Dual-SDK Planning Rule section to CLAUDE.md
date: 2026-05-24
branch: quick/260524-qdu-claude-md-dual-sdk-rule
---

# Quick Task 260524-qdu — Plan 01

## Objective

Insert a new `## Dual-SDK Planning Rule` section into `CLAUDE.md` between the existing `## Collaboration rules (Sprint 0 = lane-split)` (line 28-42) and `## Data + parity rules` (line 43). This groups it next to the existing planning/review-discipline content where `REVIEW-DISCIPLINE.md` and `CROSS-SDK-SYNC.md` are already referenced.

Single task; single file. No code, no tests.

## Tasks

### Task 1: Insert Dual-SDK Planning Rule section

<read_first>
- CLAUDE.md (current header layout; insertion point is after the last bullet of "## Collaboration rules" and before "## Data + parity rules")
- .planning/CROSS-SDK-SYNC.md (just to confirm the link path used in the new section is correct)
</read_first>

<action>
In `CLAUDE.md`, add the following new H2 section IMMEDIATELY AFTER the closing line of `## Collaboration rules (Sprint 0 = lane-split)` (ends at line 42) and BEFORE `## Data + parity rules` (starts at line 43):

```markdown
## Dual-SDK Planning Rule

Every new feature, API change, or phase plan that touches the public API surface MUST include a **TS Parity** section covering:

1. What the TS equivalent looks like (identical API, adapted implementation, or N/A).
2. Whether it ships in the same phase or gets a parity ticket per [`CROSS-SDK-SYNC.md`](.planning/CROSS-SDK-SYNC.md).
3. Any TS-specific constraints (bundle size, browser compatibility, no Node APIs).

Python-internal changes (e.g., DataFrame backend choice, parquet caching strategy) that don't affect the public API are exempt.

This applies to every PLAN.md generated by `/gsd-plan-phase`, every PROJECT.md "Active scope" update, and every ROADMAP entry that adds/changes a `tradewinds.*` or `tradewinds.weather.*` public surface. The TS Parity block lives inside the plan's frontmatter or as a top-level H2 section — reviewers (codex + python-architect + TypeScript Architect when applicable) check for its presence.
```

Then commit on the existing branch `quick/260524-qdu-claude-md-dual-sdk-rule` with message:
`docs(claude-md): add Dual-SDK Planning Rule near collaboration discipline`
</action>

<acceptance_criteria>
- `grep -n "^## Dual-SDK Planning Rule" CLAUDE.md` returns exactly 1 line, with a line number between the "## Collaboration rules" line (28) and "## Data + parity rules" (which will shift by ~12 lines after insertion).
- `grep -c "TS Parity" CLAUDE.md` returns ≥1.
- `grep -c "CROSS-SDK-SYNC.md" CLAUDE.md` returns ≥1.
- `git log -1 --format='%s' CLAUDE.md` shows the new commit subject `docs(claude-md): add Dual-SDK Planning Rule near collaboration discipline`.
- `git rev-parse --abbrev-ref HEAD` returns `quick/260524-qdu-claude-md-dual-sdk-rule`.
- No other files modified by the commit (`git show --stat HEAD` lists only `CLAUDE.md`).
</acceptance_criteria>

## Verification

```bash
# Section placement
awk '/^## /{print NR, $0}' CLAUDE.md | head -10

# Content presence
grep -A2 "^## Dual-SDK Planning Rule" CLAUDE.md | head -5

# Branch + atomic commit
git rev-parse --abbrev-ref HEAD
git show --stat HEAD
```

## Review notes

Trivial markdown insertion. Single H2 section, ~8 lines. No risk of merge conflict with `main` (touches a section main doesn't have).
