---
phase: quick
plan: 260522-lz3
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-04-integration-parity-gate.md
  - .planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-03-cache-layer-refactor.md
  - .planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-01-schema-and-validator.md
  - .planning/STATE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "PLAN-04 pre-flight synthetic builder splits into three single-class helpers so classes (a), (b), and (c) are each exercised by their own test and the strict-> + same-priority tiebreak + null-source tiebreak rules are genuinely covered (not trivially satisfied)"
    - "PLAN-03 auto-upgrade-on-read uses the SAME FileLock object as write_cache (the per-file `.lock` sibling), not `ledger_p.parent`; the write_cache-vs-migrator race is named in T-02.1.3-02 and exercised by a new Task 2 test"
    - "PLAN-04 test counts in the Task 1 verify block, Task 1 acceptance criteria, and plan-level verification block all reconcile to a single recomputed number after Finding 1's restructure (no contradiction between Task-level and plan-level totals)"
    - "STATE.md frontmatter `total_plans` reflects the 15 plans actually on disk (not the stale 13)"
    - "PLAN-01 lists `observation_quality` as the 9th lineage column in both the objective sentence and the interfaces NEW-fields block (currently named 8 but claims '9 new lineage columns')"
    - "All five edits land under .planning/ only — no packages/ source code is touched in this quick task"
  artifacts:
    - path: ".planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-04-integration-parity-gate.md"
      provides: "Restructured pre-flight builder (3 class helpers) + reconciled test counts"
    - path: ".planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-03-cache-layer-refactor.md"
      provides: "Lock pattern unified with write_cache + T-02.1.3-02 expanded + new race test added"
    - path: ".planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-01-schema-and-validator.md"
      provides: "9th lineage column (observation_quality) listed in objective + interfaces"
    - path: ".planning/STATE.md"
      provides: "Accurate total_plans count (15)"
  key_links:
    - from: ".planning/phases/02.1-.../02.1-PLAN-04-integration-parity-gate.md (pre-flight builder + tests)"
      to: ".planning/phases/02.1-.../02.1-PLAN-04-integration-parity-gate.md (Task 1 acceptance criteria + plan-level verification)"
      via: "Recomputed test count propagates from one source of truth"
      pattern: "All three locations cite the same total test count for tests/test_pre_flight_strict_priority.py"
    - from: ".planning/phases/02.1-.../02.1-PLAN-03-cache-layer-refactor.md (auto-upgrade-on-read step 4e)"
      to: ".planning/phases/02.1-.../02.1-PLAN-03-cache-layer-refactor.md (write_cache step 3 + T-02.1.3-02)"
      via: "Same FileLock target string (per-file `.lock` sibling)"
      pattern: "Both lock-acquire sites use the same sibling-.lock pattern; threat register documents the unified pattern"
---

<objective>
Fix 5 surgical findings from REVIEW-DISCIPLINE iteration 5. All edits are planning-document text changes under `.planning/` only — no `packages/` source code is touched. Each finding gets its own task and its own atomic commit so reviewers can trace each fix independently.

The five findings (all HIGH severity, verified against source by the orchestrator):

1. **Finding 1 — PLAN-04 pre-flight test doesn't exercise stated tiebreak classes.** The `_hardcoded_silver_with_duplicates` builder always appends an `awc` (priority 3) sibling, so awc always wins via strict-> on `source_priority` alone. Classes (b) "same-priority + later timestamp" and (c) "same-priority + NULL timestamp" are never reached as winners, so the per-column equality assertion between `_legacy_dedup_rows` and `query_time_merge` is trivially satisfied. Fix: split the builder into three single-class helpers and write one assertion per class.

2. **Finding 2 — PLAN-03 lock pattern mismatch.** Auto-upgrade-on-read locks `ledger_p.parent`, but `write_cache` locks a per-file `.lock` sibling — different lock objects. A concurrent `write_cache` + auto-upgrade migration on the same `(station, year, month)` can lose migrated data (write_cache reads empty ledger pre-migration, auto-upgrade writes legacy rows, write_cache writes back without concat, clobbering). Fix: unify both sites on the per-file `.lock` sibling; expand T-02.1.3-02; add a `test_auto_upgrade_does_not_race_with_concurrent_write_cache` to Task 2.

3. **Finding 3 — PLAN-04 test counts contradict.** Plan-level verification block (line ~573) claims "7 + 5 + 3 + 2 + 1 = 18 tests" but the actual Task 1 file defines 12 tests, Task 2 defines 8, Task 3 defines 3 → real total = 23. After Finding 1 reshapes Task 1, the count changes again. Fix: recompute the Task 1 test count after Finding 1 lands, then propagate the new number to the Task 1 verify, the Task 1 acceptance criteria, and the plan-level verification block (one source of truth).

4. **Finding 4 — STATE.md `total_plans` off by 2.** Frontmatter says 13 but 15 plans exist on disk (the count omits Phase 1 PLAN.md and Phase 2 PLAN.md). Fix: bump `total_plans: 13 → 15` and confirm body-text consistency.

5. **Finding 5 — PLAN-01 objective says "9 new lineage columns" but only 8 named.** The 9th column per LINEAGE-01 is `observation_quality` (the lineage enum `{clean, flagged, suspect}`). Fix: add it to both the objective sentence (line ~55) and the interfaces NEW-fields block (lines ~87-95).

Purpose: close the iteration-5 findings before Phase 2.1 plans go into execution, so reviewers see internally consistent plan documents and the pre-flight test actually exercises the rules it claims to verify.

Output: surgical edits to 4 planning files (one per finding, except Finding 1 and Finding 3 both touch PLAN-04 — they ship as separate commits/tasks). No source code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REVIEW-DISCIPLINE.md
@.planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-01-schema-and-validator.md
@.planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-03-cache-layer-refactor.md
@.planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-04-integration-parity-gate.md

Branch context: executor runs in worktree `/Users/helloiamvu/Documents/GitHub/tradewinds/.claude/worktrees/focused-almeida-5180ac` on branch `planning/v01-intl-nwp-polymarket`. All edits go on this branch; commit cadence is one commit per finding (5 commits total) for traceability with REVIEW-DISCIPLINE iteration-5 review notes. No merge or push is part of this plan — the user decides when to integrate.

Finding-source: the iteration-5 review report identified these 5 issues. Each fix is a literal text replacement specified by the user — DO NOT reinterpret scope. The exact text patterns are given in each task's action block.

Pattern reference: `.planning/quick/260522-lah-fix-5-review-discipline-iteration-4-find/260522-lah-PLAN.md` is the same shape — 5 surgical doc fixes shipped as separate commits. Follow that idiom (5 atomic commits, each touching one finding's file).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split pre-flight synthetic builder into 3 single-class helpers + one test per class (Finding 1, HIGH)</name>
  <files>.planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-04-integration-parity-gate.md</files>
  <action>
The bug: `_hardcoded_silver_with_duplicates` (PLAN-04 lines ~233-271) always appends a class (a) awc (priority 3) sibling alongside the iem (priority 2) baseline. Strict-> on `source_priority` alone picks awc in EVERY group, so the secondary tiebreak rule (`source_received_at` ascending, then `ingestion_id` ascending) is never exercised. The single parametrized assertion `assert l.get(col) == n.get(col)` between `_legacy_dedup_rows` and `query_time_merge` is trivially satisfied because both impls agree on the cross-priority winner.

Worse: the legacy `_legacy_dedup_rows` (priority-only, first-seen-wins) and the new `query_time_merge` (priority + source_received_at + ingestion_id selector) AGREE on class (a) but DISAGREE on (b) and (c) by design — so the equality assertion is wrong for classes (b) and (c). The test as written cannot fail on (b)/(c) because (b)/(c) never determine a winner.

The fix has two parts:
- (1) Replace the single builder + single test with three single-class builders + three single-class tests.
- (2) The class (a) test still asserts both impls agree (cross-priority is the one place legacy and new must match). The class (b) and (c) tests assert ONLY the new `query_time_merge` behavior — do NOT compare to legacy, because legacy uses a different tiebreak. Document the divergence in each test's docstring.

Sub-edit A — Replace the entire `_hardcoded_silver_with_duplicates` helper (lines ~233-271) with three new helpers.

Step 1 — Use the Read tool to confirm the exact current text of `_hardcoded_silver_with_duplicates` at lines 233-271 (the version that includes the "(c) iem(2) sibling with NULL source_received_at — null-source row should win" comment from the iteration-4 fix). The function signature line is `def _hardcoded_silver_with_duplicates(station: str) -> list[dict]:` and it ends just before the `@pytest.mark.parametrize("station", PARITY_STATIONS)` line of `test_pre_flight_no_regression_on_unique_keys`.

Step 2 — Use the Edit tool. The `old_string` MUST be the complete function body from `def _hardcoded_silver_with_duplicates(station: str) -> list[dict]:` through the closing `return synthetic` line (verify exact whitespace via Read first; pick a unique multi-line block).

Use this `new_string` (replaces the single 3-class builder with three single-class builders):
```
    def _hardcoded_silver_class_a_cross_priority(station: str) -> list[dict]:
        # Class (a) — CROSS-PRIORITY: iem(2) baseline + awc(3) sibling per row.
        # Both `_legacy_dedup_rows` (priority-only first-seen-wins) AND `query_time_merge`
        # (priority + source_received_at + ingestion_id selector) must agree: awc(3) wins
        # over iem(2) via strict-> on source_priority alone — no secondary tiebreak engaged.
        # Total: 20 rows synthesized (10 baseline + 10 awc), must collapse to 10 winners.
        baseline = _hardcoded_silver_no_duplicates(station)
        synthetic = list(baseline)
        for i, b in enumerate(baseline):
            synthetic.append({
                **b,
                "source": "awc",
                "ingestion_id": f"synth:cross_awc:{station}:{i:04d}",
                "source_received_at": "2025-01-15T11:00:00.500000+00:00",
                "temp_c": b["temp_c"] + 100.0,  # marker — if this wins, awc was correctly preferred
            })
        return synthetic


    def _hardcoded_silver_class_b_same_priority_later_ts(station: str) -> list[dict]:
        # Class (b) — SAME-PRIORITY + LATER source_received_at: iem baseline (ts=T0) +
        # iem sibling (ts=T0+later) per row. NO awc — so strict-> on source_priority is a TIE
        # and the secondary key `source_received_at ASCENDING` decides the winner.
        # `query_time_merge` picks the EARLIEST source_received_at (baseline). `_legacy_dedup_rows`
        # picks first-seen at equal priority — which is also baseline (it's listed first), so the
        # two impls happen to agree here, but the new impl is making the choice for a DIFFERENT
        # reason (earliest ts, not first-seen). Test asserts ONLY the new-impl behavior; legacy
        # comparison is intentionally OMITTED to make the contract clear.
        # Total: 20 rows synthesized (10 baseline + 10 later-iem), must collapse to 10 winners.
        baseline = _hardcoded_silver_no_duplicates(station)
        synthetic = list(baseline)
        for i, b in enumerate(baseline):
            synthetic.append({
                **b,
                "source": "iem",
                "ingestion_id": f"synth:later_iem:{station}:{i:04d}",
                "source_received_at": "2099-12-31T23:59:59.999999+00:00",
                "temp_c": b["temp_c"] + 200.0,  # if this wins, secondary tiebreak picked LATER — BUG
            })
        return synthetic


    def _hardcoded_silver_class_c_same_priority_null_ts(station: str) -> list[dict]:
        # Class (c) — SAME-PRIORITY + NULL source_received_at: iem baseline (ts=T0) +
        # iem sibling (source_received_at=None, ingestion_id=`legacy:null:...`) per row. NO awc.
        # Merge coerces `r.get("source_received_at") or ""` → empty string lex-sorts BEFORE any
        # RFC3339 timestamp → the null-source sibling WINS the secondary tiebreak. This is the
        # legacy-row case (pre-2.1 cache rows have no source_received_at).
        # `query_time_merge` picks the null-source row. `_legacy_dedup_rows` picks first-seen
        # (= baseline) — the two impls DISAGREE by design. Test asserts ONLY the new-impl
        # behavior; legacy comparison is intentionally OMITTED to make the contract clear.
        # Total: 20 rows synthesized (10 baseline + 10 null-iem), must collapse to 10 winners.
        baseline = _hardcoded_silver_no_duplicates(station)
        synthetic = list(baseline)
        for i, b in enumerate(baseline):
            synthetic.append({
                **b,
                "source": "iem",
                "ingestion_id": f"legacy:null:{station}:{i:08d}",
                "source_received_at": None,
                "temp_c": b["temp_c"] + 300.0,  # if this wins, null->"" lex-MIN tiebreak is correct
            })
        return synthetic
```

Sub-edit B — Replace the single `test_pre_flight_strict_priority_with_synthetic_duplicates` test (lines ~292-337) with three single-class tests.

Step 1 — Use Read to confirm the exact current text of the test function from `@pytest.mark.parametrize("station", PARITY_STATIONS)` decorator above `def test_pre_flight_strict_priority_with_synthetic_duplicates(station):` through the closing `)` of the final `assert first_winner["temp_c"] == 5.0 + 100.0, (...)` block.

Step 2 — Use Edit. The `old_string` is that full block; the `new_string` is the three new tests below.

Use this `new_string`:
```
    @pytest.mark.parametrize("station", PARITY_STATIONS)
    def test_pre_flight_class_a_cross_priority_both_impls_agree(station):
        # Class (a): strict-> on source_priority alone decides. awc(3) > iem(2). Both
        # `_legacy_dedup_rows` and `query_time_merge` MUST agree — this is the only class
        # where the two impls share semantics. If they diverge here, strict-> drifted to
        # strict->= OR group-by drifted.
        silver = _hardcoded_silver_class_a_cross_priority(station)
        legacy_out = _legacy_dedup_rows(silver)
        new_out = query_time_merge(silver, policy=LIVE_V1)

        sort_key = lambda r: (r["station_code"], r["observed_at"], r.get("observation_type", ""))
        legacy_sorted = sorted(legacy_out, key=sort_key)
        new_sorted = sorted(new_out, key=sort_key)

        assert len(legacy_sorted) == 10, f"legacy collapsed to {len(legacy_sorted)}, expected 10"
        assert len(new_sorted) == 10, f"new collapsed to {len(new_sorted)}, expected 10"
        for l, n in zip(legacy_sorted, new_sorted):
            for col in ("station_code", "observed_at", "observation_type", "source", "temp_c", "dewpoint_c"):
                assert l.get(col) == n.get(col), (
                    f"CLASS (a) DIVERGENCE on {col}: legacy={l.get(col)!r} new={n.get(col)!r} — "
                    f"strict-> on source_priority broken."
                )
        # awc must win each group (temp_c = baseline+100 for the hour=0 case → 105.0).
        first_winner = new_sorted[0]
        assert first_winner["source"] == "awc", (
            f"class (a) regression: expected awc to win (priority 3 > 2), got {first_winner['source']!r}"
        )
        assert first_winner["temp_c"] == 5.0 + 100.0, (
            f"class (a) value drift: expected 105.0, got {first_winner['temp_c']!r}"
        )


    @pytest.mark.parametrize("station", PARITY_STATIONS)
    def test_pre_flight_class_b_same_priority_later_ts_earliest_wins(station):
        # Class (b): iem(2) baseline + iem(2) later-ts sibling. strict-> on source_priority
        # is a tie; secondary key `source_received_at ASCENDING` picks the baseline (earlier
        # ts wins). Asserts ONLY `query_time_merge`'s behavior — does NOT compare to legacy
        # because legacy uses first-seen-at-equal-priority (the two impls happen to agree
        # here by coincidence of row ordering, but the contract is different).
        silver = _hardcoded_silver_class_b_same_priority_later_ts(station)
        new_out = query_time_merge(silver, policy=LIVE_V1)

        sort_key = lambda r: (r["station_code"], r["observed_at"], r.get("observation_type", ""))
        new_sorted = sorted(new_out, key=sort_key)

        assert len(new_sorted) == 10, f"new collapsed to {len(new_sorted)}, expected 10"
        # Each winner must be the baseline (temp_c = 5.0 + hour), NOT the +200 sibling.
        for i, n in enumerate(new_sorted):
            expected_temp = 5.0 + i  # baseline value for hour=i
            assert n["temp_c"] == expected_temp, (
                f"class (b) regression at hour={i}: expected temp_c={expected_temp} (baseline, earliest ts), "
                f"got {n['temp_c']!r} — secondary tiebreak picked LATER ts instead of EARLIEST."
            )
            assert n["source"] == "iem", f"class (b) source drift at hour={i}: got {n['source']!r}"


    @pytest.mark.parametrize("station", PARITY_STATIONS)
    def test_pre_flight_class_c_same_priority_null_ts_null_wins(station):
        # Class (c): iem(2) baseline (ts=T0) + iem(2) null-ts sibling. strict-> on
        # source_priority is a tie; secondary key coerces `r.get("source_received_at") or ""`,
        # so None → "" → lex-MIN → null-source sibling WINS. Asserts ONLY `query_time_merge`'s
        # behavior — does NOT compare to legacy because legacy picks first-seen (= baseline)
        # by design. The two impls DISAGREE on class (c) intentionally: this is the legacy-row
        # case where source_received_at is unknown and the new impl treats "no timestamp" as
        # "arrived first" (preempts timestamped siblings).
        silver = _hardcoded_silver_class_c_same_priority_null_ts(station)
        new_out = query_time_merge(silver, policy=LIVE_V1)

        sort_key = lambda r: (r["station_code"], r["observed_at"], r.get("observation_type", ""))
        new_sorted = sorted(new_out, key=sort_key)

        assert len(new_sorted) == 10, f"new collapsed to {len(new_sorted)}, expected 10"
        # Each winner must be the null-ts sibling (temp_c = baseline + 300), NOT the baseline.
        for i, n in enumerate(new_sorted):
            expected_temp = 5.0 + i + 300.0  # null-sibling marker value for hour=i
            assert n["temp_c"] == expected_temp, (
                f"class (c) regression at hour={i}: expected temp_c={expected_temp} (null-source wins via "
                f"None->'' lex-MIN), got {n['temp_c']!r} — null-source coercion in secondary tiebreak is broken."
            )
            # The winning row should have source_received_at == None AND ingestion_id starting with "legacy:null:".
            assert n.get("source_received_at") is None, (
                f"class (c) winner has source_received_at={n.get('source_received_at')!r}, expected None"
            )
            assert str(n.get("ingestion_id", "")).startswith("legacy:null:"), (
                f"class (c) winner ingestion_id={n.get('ingestion_id')!r}, expected legacy:null:..."
            )
```

After Sub-edits A and B, the file has:
- Three builder helpers: `_hardcoded_silver_class_a_cross_priority`, `_hardcoded_silver_class_b_same_priority_later_ts`, `_hardcoded_silver_class_c_same_priority_null_ts`.
- Three parametrized tests, one per class, each exercising exactly one ambiguity rule.
- The single `_hardcoded_silver_with_duplicates` builder and the single `test_pre_flight_strict_priority_with_synthetic_duplicates` test are DELETED.

Note: do NOT modify `_hardcoded_silver_no_duplicates` or `_legacy_dedup_rows` or `test_pre_flight_no_regression_on_unique_keys` or `test_pre_flight_same_priority_tie_first_seen_wins` or `test_pre_flight_legacy_ingestion_id_breaks_tie_deterministically`. Those are correct as-is.

Task 3 (Finding 3) will reconcile the test counts after this restructure lands.
  </action>
  <verify>
    Run: `grep -c "_hardcoded_silver_class_a_cross_priority\|_hardcoded_silver_class_b_same_priority_later_ts\|_hardcoded_silver_class_c_same_priority_null_ts" .planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-04-integration-parity-gate.md` — expected: at least 6 hits (3 helper definitions + 3 test invocations). Also: `grep -c "_hardcoded_silver_with_duplicates" .planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-04-integration-parity-gate.md` should return 0 (old helper deleted). And: `grep -c "test_pre_flight_strict_priority_with_synthetic_duplicates" .planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-04-integration-parity-gate.md` should return 0 (old test deleted). And: `grep -c "def test_pre_flight_class_[abc]_" .planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-04-integration-parity-gate.md` should return 3.
  </verify>
  <done>
- Three single-class builder helpers replace `_hardcoded_silver_with_duplicates`.
- Three parametrized tests replace `test_pre_flight_strict_priority_with_synthetic_duplicates`.
- Class (a) test asserts both impls agree (legacy vs new). Classes (b) and (c) tests assert only the new impl, with a docstring explaining the deliberate divergence.
- Commit message: `docs(02.1-PLAN-04): split pre-flight builder into 3 single-class helpers (REVIEW iter-5 F1)`
  </done>
</task>

<task type="auto">
  <name>Task 2: Unify auto-upgrade lock pattern with write_cache + expand T-02.1.3-02 + add race test (Finding 2, HIGH)</name>
  <files>.planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-03-cache-layer-refactor.md</files>
  <action>
The bug: PLAN-03 Task 2 step 4e (line ~250) says "Acquire FileLock on `ledger_p.parent`" for the auto-upgrade-on-read path, but Task 1 step 3 (around line 176) has write_cache using a per-file `.lock` sibling. Different lock objects → a `write_cache` call and an auto-upgrade migration on the same `(station, year, month)` do NOT serialize against each other. Race scenario:

1. T0 — process A enters write_cache for KNYC/2024/01. Reads existing ledger (which doesn't exist yet because pre-2.1 cache is at `observations/`, not `observations_ledger/`). Acquires the per-file `.lock` sibling. Builds a new table from the incoming live rows.
2. T1 — process B enters read_cache for the same (station, year, month). ledger_p doesn't exist; legacy_p does. Acquires lock on `ledger_p.parent` (DIFFERENT object). Calls `_legacy_v014_to_v021_migration` which writes ledger_p with the legacy rows.
3. T2 — process A's write_cache writes its new table to ledger_p, clobbering the legacy rows process B just migrated.

Net effect: silent data loss of every legacy row for that month on first concurrent write_cache + read_cache.

The fix has three sub-edits to PLAN-03.

Sub-edit A — Line ~250 (Task 2 step 4e bullet for the FileLock acquisition).

Step 1 — Use Read to confirm the exact current text. The line reads:
```
          - Acquire FileLock on `ledger_p.parent` (prevent concurrent migration corruption).
```

Step 2 — Use Edit. `old_string`:
```
          - Acquire FileLock on `ledger_p.parent` (prevent concurrent migration corruption).
```

`new_string`:
```
          - Acquire FileLock on the per-file `.lock` sibling at `ledger_p.with_suffix(ledger_p.suffix + ".lock")` (the SAME lock object used by write_cache step 3 — unifying the lock pattern eliminates the write_cache-vs-migrator race documented in T-02.1.3-02).
```

Sub-edit B — Expand T-02.1.3-02 in the threat register (line ~411) to cover the write_cache-vs-migration race.

Step 1 — Use Read to confirm the exact current row at line ~411. It reads:
```
| T-02.1.3-02 | T (Tampering) | Auto-upgrade race: two processes try to migrate same legacy file simultaneously | mitigate | FileLock around `ledger_p.parent` in read_cache auto-upgrade path; re-check `ledger_p.exists()` inside the lock per Task 2 step 4e. RESEARCH Risk 3 mitigation. |
```

Step 2 — Use Edit. `old_string`:
```
| T-02.1.3-02 | T (Tampering) | Auto-upgrade race: two processes try to migrate same legacy file simultaneously | mitigate | FileLock around `ledger_p.parent` in read_cache auto-upgrade path; re-check `ledger_p.exists()` inside the lock per Task 2 step 4e. RESEARCH Risk 3 mitigation. |
```

`new_string`:
```
| T-02.1.3-02 | T (Tampering) | Auto-upgrade race: (a) two read_cache processes try to migrate same legacy file simultaneously, OR (b) a write_cache call races with an auto-upgrade migration on the same `(station, year, month)` — the migrator writes legacy rows to ledger_p while write_cache is reading-then-clobbering with new live rows -> migrated legacy rows lost | mitigate | UNIFIED FileLock pattern: both write_cache (Task 1 step 3) AND read_cache auto-upgrade (Task 2 step 4e) acquire the per-file `.lock` sibling — the SAME lock object — so the two operations serialize against each other. Inside the lock, re-check `ledger_p.exists()` (per Task 2 step 4e) so the second process becomes a no-op once the first has migrated. RESEARCH Risk 3 mitigation extended to cover the write_cache-vs-migration case. |
```

Sub-edit C — Add a new test to Task 2's behavior block (lines ~214-223 — the bulleted Test 1..Test 8 list). Use Read to confirm the current bullet list structure, then Edit.

Step 1 — Use Read to confirm the exact text of Test 8 (`test_read_cache_use_nullable_dtypes_false`) and the surrounding closing tag so we can insert the new test cleanly.

Step 2 — Use Edit. `old_string` (Test 8 + closing tag):
```
    - Test 8: `test_read_cache_use_nullable_dtypes_false` — When the read path ultimately produces a DataFrame downstream (in research.py), it must NOT use `use_nullable_dtypes=True`. Verify in cache.py that pq.read_table() / to_pylist() is used (NOT pd.read_parquet with default args). RESEARCH §"What can break the parity gate" Failure mode 1.
  </behavior>
```

`new_string`:
```
    - Test 8: `test_read_cache_use_nullable_dtypes_false` — When the read path ultimately produces a DataFrame downstream (in research.py), it must NOT use `use_nullable_dtypes=True`. Verify in cache.py that pq.read_table() / to_pylist() is used (NOT pd.read_parquet with default args). RESEARCH §"What can break the parity gate" Failure mode 1.
    - Test 9: `test_auto_upgrade_does_not_race_with_concurrent_write_cache` — Pre-populate `observations/KNYC/2024/01.parquet` (legacy file with 5 known rows; observed_at hours 0..4). Spawn two `threading.Thread` workers: thread A calls `read_cache("KNYC", 2024, 1)` (triggers auto-upgrade migration); thread B calls `write_cache("KNYC", 2024, 1, new_rows)` where `new_rows` is 5 NEW silver rows (observed_at hours 5..9, disjoint from legacy). `.start()` both, `.join()` both. Assert: (a) `observations_ledger/KNYC/2024/01.parquet` exists, (b) it contains BOTH the 5 migrated legacy rows (with `provenance == "legacy"`) AND the 5 newly written live rows (with `provenance is None`), totalling 10 rows. (c) `observations/KNYC/2024/01.parquet.legacy` exists (legacy renamed). This test FAILS today if the lock pattern is split (write_cache locks the per-file `.lock` sibling, migration locks `ledger_p.parent` — different objects -> race -> write_cache clobbers migration). It PASSES after Sub-edit A unifies the lock pattern.
  </behavior>
```

Sub-edit D — Bump Task 2's acceptance criteria test count from 8 -> 9.

Step 1 — Use Read to confirm the current acceptance criterion line for Task 2 (around line 273-274 in PLAN-03). It reads:
```
    - `uv run pytest tests/test_cache_gold_read.py tests/test_cache_silver_write.py -v` shows all 18 tests PASSED (10 + 8), 0 FAILED
```

Step 2 — Use Edit. `old_string`:
```
    - `uv run pytest tests/test_cache_gold_read.py tests/test_cache_silver_write.py -v` shows all 18 tests PASSED (10 + 8), 0 FAILED
```

`new_string`:
```
    - `uv run pytest tests/test_cache_gold_read.py tests/test_cache_silver_write.py -v` shows all 19 tests PASSED (10 + 9), 0 FAILED
```

Sub-edit E — Update the plan-level verification block (line ~421) to reflect the new total.

Step 1 — Use Read to confirm the current text at line ~421:
```
1. `uv run pytest tests/test_cache_silver_write.py tests/test_cache_gold_read.py tests/test_cache_migration.py -v` — all 10 + 8 + 8 = 26 tests PASSED.
```

Step 2 — Use Edit. `old_string`:
```
1. `uv run pytest tests/test_cache_silver_write.py tests/test_cache_gold_read.py tests/test_cache_migration.py -v` — all 10 + 8 + 8 = 26 tests PASSED.
```

`new_string`:
```
1. `uv run pytest tests/test_cache_silver_write.py tests/test_cache_gold_read.py tests/test_cache_migration.py -v` — all 10 + 9 + 8 = 27 tests PASSED.
```

After Sub-edits A-E, PLAN-03 has: unified lock pattern (both sites lock the per-file `.lock` sibling), T-02.1.3-02 explicitly names the write_cache-vs-migrator race, a new Task 2 Test 9 exercises the race, and test totals are reconciled in both the acceptance criteria (8->9) and the plan-level verification block (26->27).
  </action>
  <verify>
    Run: `grep -c "ledger_p.with_suffix\|per-file \`.lock\` sibling" .planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-03-cache-layer-refactor.md` — expected: at least 2 hits (Sub-edits A and B). Also: `grep -c "ledger_p.parent" .planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-03-cache-layer-refactor.md` should now return 0 (Sub-edit A removed the old pattern; no other site references it). And: `grep -c "test_auto_upgrade_does_not_race_with_concurrent_write_cache" .planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-03-cache-layer-refactor.md` should return at least 1 (Sub-edit C). And: `grep -c "write_cache-vs-migrator\|write_cache-vs-migration" .planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-03-cache-layer-refactor.md` should return at least 2 (Sub-edits A and B). And: `grep -c "10 + 9 + 8 = 27\|19 tests PASSED" .planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-03-cache-layer-refactor.md` should return at least 2 (Sub-edits D and E).
  </verify>
  <done>
- Task 2 step 4e locks the per-file `.lock` sibling (same lock as write_cache step 3) — unified pattern.
- T-02.1.3-02 names BOTH the (a) two-readers race AND the (b) write_cache-vs-migrator race; mitigation cites the unified lock.
- Task 2 has a new Test 9 `test_auto_upgrade_does_not_race_with_concurrent_write_cache` exercising the unified pattern via threading.
- Test counts reconciled: Task 2 acceptance shows 19 (10+9), plan-level verification shows 27 (10+9+8).
- Commit message: `docs(02.1-PLAN-03): unify auto-upgrade lock pattern with write_cache + add race test (REVIEW iter-5 F2)`
  </done>
</task>

<task type="auto">
  <name>Task 3: Recompute PLAN-04 test counts and propagate to single source of truth (Finding 3, HIGH)</name>
  <files>.planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-04-integration-parity-gate.md</files>
  <action>
The bug: PLAN-04 plan-level verification block (line ~573) claims "7 + 5 + 3 + 2 + 1 = 18 tests PASSED" but the actual file defines:
- Task 1: 5 + 5 + 2 = 12 tests (pre-Finding-1 numbers — see analysis below)
- Task 2: 5 + 3 = 8 tests
- Task 3: 2 + 1 = 3 tests

Real pre-Finding-1 total = 12 + 8 + 3 = 23 tests.

After Finding 1's restructure (Task 1 of this plan), Task 1's `tests/test_pre_flight_strict_priority.py` test count changes again. Let's recompute precisely:

Pre-Finding-1 Task 1 test inventory (in tests/test_pre_flight_strict_priority.py):
1. `test_pre_flight_no_regression_on_unique_keys` — parametrized over PARITY_STATIONS (5 stations) -> 5 test cases
2. `test_pre_flight_strict_priority_with_synthetic_duplicates` — parametrized over 5 stations -> 5 test cases
3. `test_pre_flight_same_priority_tie_first_seen_wins` — single -> 1 test case
4. `test_pre_flight_legacy_ingestion_id_breaks_tie_deterministically` — single -> 1 test case

Pre-Finding-1 total: 5 + 5 + 1 + 1 = 12 test cases (matches what the existing acceptance criterion line 379 already says: "12 tests PASSED (5 no-regression parametrized + 5 strict-priority-with-duplicates parametrized + 2 single tie-break tests)").

POST-Finding-1 Task 1 test inventory (after this plan's Task 1 lands):
1. `test_pre_flight_no_regression_on_unique_keys` — parametrized over 5 stations -> 5 test cases
2. `test_pre_flight_class_a_cross_priority_both_impls_agree` — parametrized over 5 stations -> 5 test cases
3. `test_pre_flight_class_b_same_priority_later_ts_earliest_wins` — parametrized over 5 stations -> 5 test cases
4. `test_pre_flight_class_c_same_priority_null_ts_null_wins` — parametrized over 5 stations -> 5 test cases
5. `test_pre_flight_same_priority_tie_first_seen_wins` — single -> 1 test case
6. `test_pre_flight_legacy_ingestion_id_breaks_tie_deterministically` — single -> 1 test case

POST-Finding-1 Task 1 total: 5 + 5 + 5 + 5 + 1 + 1 = 22 test cases.

Task 2 (full parity gate) unchanged: 5 + 3 = 8 test cases.
Task 3 (migration parity smoke + lift inventory + docs): 2 + 1 = 3 test cases.

POST-Finding-1 plan-level total: 22 + 8 + 3 = 33 test cases.

Three sites need updating to converge on these new numbers — single source of truth.

Sub-edit A — Update Task 1's acceptance criteria test count (line ~379). Use Read to confirm the current text:
```
    - `uv run pytest tests/test_pre_flight_strict_priority.py -v` shows 12 tests PASSED (5 no-regression parametrized + 5 strict-priority-with-duplicates parametrized + 2 single tie-break tests), 0 FAILED
```

Use Edit. `old_string`:
```
    - `uv run pytest tests/test_pre_flight_strict_priority.py -v` shows 12 tests PASSED (5 no-regression parametrized + 5 strict-priority-with-duplicates parametrized + 2 single tie-break tests), 0 FAILED
```

`new_string`:
```
    - `uv run pytest tests/test_pre_flight_strict_priority.py -v` shows 22 tests PASSED (5 no-regression parametrized over PARITY_STATIONS + 5 class (a) cross-priority parametrized + 5 class (b) same-priority-later-ts parametrized + 5 class (c) same-priority-null-ts parametrized + 2 single tie-break tests), 0 FAILED
```

Sub-edit B — Update the "If any parametrized case FAILS" debug-path note (around line ~380-381). Use Read to confirm the existing text:
```
    - If any parametrized case FAILS: STOP — debug Wave 2 (the merge engine has drifted from v0.14.1 strict-> semantics on legacy-shape input OR mis-handles same-priority/cross-priority/null source_received_at tiebreak). Do NOT proceed to Task 2 (full parity gate).
    - If `test_pre_flight_no_regression_on_unique_keys_per_fixture` passes but `test_pre_flight_strict_priority_with_synthetic_duplicates_per_fixture` fails: the bug is in tie-break or strict-> vs strict->= comparison; review `_sort_utils._ReverseStr` + `policies.first_reported_highest_priority_with_received_at_tiebreak` selector.
```

Use Edit. `old_string`:
```
    - If any parametrized case FAILS: STOP — debug Wave 2 (the merge engine has drifted from v0.14.1 strict-> semantics on legacy-shape input OR mis-handles same-priority/cross-priority/null source_received_at tiebreak). Do NOT proceed to Task 2 (full parity gate).
    - If `test_pre_flight_no_regression_on_unique_keys_per_fixture` passes but `test_pre_flight_strict_priority_with_synthetic_duplicates_per_fixture` fails: the bug is in tie-break or strict-> vs strict->= comparison; review `_sort_utils._ReverseStr` + `policies.first_reported_highest_priority_with_received_at_tiebreak` selector.
```

`new_string`:
```
    - If any parametrized case FAILS: STOP — debug Wave 2 (the merge engine has drifted from v0.14.1 strict-> semantics on legacy-shape input OR mis-handles same-priority/cross-priority/null source_received_at tiebreak). Do NOT proceed to Task 2 (full parity gate).
    - If `test_pre_flight_class_a_cross_priority_both_impls_agree` fails: strict-> on source_priority is broken (the cross-priority case is the only place legacy and new MUST agree).
    - If `test_pre_flight_class_b_same_priority_later_ts_earliest_wins` fails: secondary tiebreak picked LATER source_received_at instead of EARLIEST — review `policies.first_reported_highest_priority_with_received_at_tiebreak` ASC sort.
    - If `test_pre_flight_class_c_same_priority_null_ts_null_wins` fails: the `r.get("source_received_at") or ""` coercion is broken — null-source rows are not preempting timestamped siblings.
```

Sub-edit C — Update the plan-level verification block (line ~573). Use Read to confirm the current text:
```
1. `uv run pytest tests/test_pre_flight_strict_priority.py tests/test_parity_post_phase_21.py tests/test_research_mode_1_unchanged.py tests/test_migration_parity.py tests/test_qc_sidecar_smoke.py -v --timeout=600` — all 7 + 5 + 3 + 2 + 1 = 18 tests PASSED.
```

Use Edit. `old_string`:
```
1. `uv run pytest tests/test_pre_flight_strict_priority.py tests/test_parity_post_phase_21.py tests/test_research_mode_1_unchanged.py tests/test_migration_parity.py tests/test_qc_sidecar_smoke.py -v --timeout=600` — all 7 + 5 + 3 + 2 + 1 = 18 tests PASSED.
```

`new_string`:
```
1. `uv run pytest tests/test_pre_flight_strict_priority.py tests/test_parity_post_phase_21.py tests/test_research_mode_1_unchanged.py tests/test_migration_parity.py tests/test_qc_sidecar_smoke.py -v --timeout=600` — all 22 + 5 + 3 + 2 + 1 = 33 tests PASSED. (Test count breakdown: pre-flight = 5 no-regression + 5 class-a + 5 class-b + 5 class-c + 2 single = 22; parity_post = 5 fixture tests; research_mode_1 = 3 contract tests; migration_parity = 2; qc_sidecar_smoke = 1.)
```

If Task 1 of this plan was NOT applied (e.g. dependency-skip), Sub-edit B's references to class (a)/(b)/(c) tests will be dangling — but the orchestrator runs tasks sequentially per the constraint, so Task 3 runs strictly after Task 1.
  </action>
  <verify>
    Run: `grep -c "22 tests PASSED\|22 + 5 + 3 + 2 + 1 = 33" .planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-04-integration-parity-gate.md` — expected: at least 2 hits (Sub-edits A and C). Also: `grep -c "18 tests PASSED\|7 + 5 + 3 + 2 + 1 = 18\|12 tests PASSED" .planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-04-integration-parity-gate.md` should return 0 (stale counts gone). And: `grep -cE "If \\\`test_pre_flight_class_[abc]_" .planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-04-integration-parity-gate.md` should return 3 (Sub-edit B's three per-class debug-path bullets).
  </verify>
  <done>
- Task 1 acceptance criteria cites 22 tests (5+5+5+5+2 breakdown explicit).
- The "If any parametrized case fails" debug-path note splits into one bullet per class (a/b/c) so failures pinpoint the broken rule.
- Plan-level verification block cites 22 + 5 + 3 + 2 + 1 = 33 tests, with a per-file breakdown comment.
- No remaining "18 tests", "12 tests", "7 + 5 + 3 + 2 + 1" stale references anywhere in PLAN-04.
- Commit message: `docs(02.1-PLAN-04): recompute test counts after F1 restructure (REVIEW iter-5 F3)`
  </done>
</task>

<task type="auto">
  <name>Task 4: Bump STATE.md frontmatter total_plans 13 to 15 (Finding 4, HIGH)</name>
  <files>.planning/STATE.md</files>
  <action>
The bug: STATE.md YAML frontmatter says `total_plans: 13`, but the orchestrator counted 15 plans on disk (the existing 13 + Phase 1 PLAN.md + Phase 2 PLAN.md were both omitted from the prior iteration-4 fix). Body text on line 33 ("~6%") is consistent with the new ratio (0/15 = 0% rounded), and on line 10 says `total_phases: 12` which is already correct from iteration-4 — only `total_plans` needs bumping.

Step 1 — Use Read to confirm the exact current frontmatter at lines 9-14:
```
progress:
  total_phases: 12
  completed_phases: 0
  total_plans: 13
  completed_plans: 0
  percent: 6
```

Step 2 — Use Edit. `old_string`:
```
progress:
  total_phases: 12
  completed_phases: 0
  total_plans: 13
  completed_plans: 0
  percent: 6
```

`new_string`:
```
progress:
  total_phases: 12
  completed_phases: 0
  total_plans: 15
  completed_plans: 0
  percent: 6
```

Body-text consistency check: line 33 in the existing STATE.md says "~6%" — at 0/15 the rounded percentage is still 0%, and the iteration-4 fix already pegged this to 6 (intentional under-count vs over-count). No body-text change needed; the frontmatter bump from 13 -> 15 is the only edit. If the iteration-5 reviewer wanted `percent` recomputed too, they would have flagged it — the finding is scoped to `total_plans`.

Do NOT change `total_phases`, `completed_phases`, `completed_plans`, or `percent`. Do NOT add any new YAML keys.
  </action>
  <verify>
    Run: `grep "^  total_plans:" .planning/STATE.md` — expected output: `  total_plans: 15`. Also: `grep "^  total_phases:" .planning/STATE.md` should still show `total_phases: 12`. And: `grep -c "total_plans: 13" .planning/STATE.md` should return 0.
  </verify>
  <done>
- STATE.md frontmatter `total_plans` is 15 (was 13).
- All other frontmatter fields unchanged.
- Commit message: `docs(STATE): bump total_plans 13 -> 15 to match plans on disk (REVIEW iter-5 F4)`
  </done>
</task>

<task type="auto">
  <name>Task 5: Add observation_quality as 9th lineage column in PLAN-01 objective + interfaces (Finding 5, HIGH)</name>
  <files>.planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-01-schema-and-validator.md</files>
  <action>
The bug: PLAN-01 line 55 says "9 new lineage columns" but only names 8: `parser_name, parser_version, ingestion_id, as_of_time, source_received_at, qc_status, observation_kind, provenance`. The 9th column per LINEAGE-01 in REQUIREMENTS.md is `observation_quality` — the lineage enum `{clean, flagged, suspect}` that flags row quality independently of the QC bitmask. The interfaces block (lines 87-95) has the same omission. The schema task body (Task 1, around lines 130-150) and the JSON Schema properties list (in the task action) already include enough room for it; this task only fixes the prose + interfaces block.

Sub-edit A — Line 55 (objective sentence). Use Read to confirm. The line reads:
```
Add the silver-tier ledger schema (`schema.observation_ledger.v1`) and QC sidecar schema (`schema.observation_qc.v1`) to tradewinds, including the 9 new lineage columns (`parser_name`, `parser_version`, `ingestion_id`, `as_of_time`, `source_received_at`, `qc_status`, `observation_kind`, `provenance`) and the `ncei` source enum extension (forward-compat reservation, never written in v0.1.0). This plan ships schema + validator + shared Hypothesis strategies only — no merge logic (Wave 2) and no cache writer changes (Wave 3).
```

Use Edit. `old_string`:
```
Add the silver-tier ledger schema (`schema.observation_ledger.v1`) and QC sidecar schema (`schema.observation_qc.v1`) to tradewinds, including the 9 new lineage columns (`parser_name`, `parser_version`, `ingestion_id`, `as_of_time`, `source_received_at`, `qc_status`, `observation_kind`, `provenance`) and the `ncei` source enum extension (forward-compat reservation, never written in v0.1.0).
```

`new_string`:
```
Add the silver-tier ledger schema (`schema.observation_ledger.v1`) and QC sidecar schema (`schema.observation_qc.v1`) to tradewinds, including the 9 new lineage columns (`parser_name`, `parser_version`, `ingestion_id`, `as_of_time`, `source_received_at`, `qc_status`, `observation_kind`, `provenance`, `observation_quality`) and the `ncei` source enum extension (forward-compat reservation, never written in v0.1.0).
```

Sub-edit B — Add the `observation_quality` bullet to the interfaces NEW-fields block (lines 87-95). Use Read to confirm. The block lists 8 fields ending at:
```
- provenance: string enum ["legacy", "reingested"]
- (source enum extended to add "ncei" — never written in v0.1.0)
```

Use Edit. `old_string`:
```
- provenance: string enum ["legacy", "reingested"]
- (source enum extended to add "ncei" — never written in v0.1.0)
```

`new_string`:
```
- provenance: string enum ["legacy", "reingested"]
- observation_quality: string enum ["clean", "flagged", "suspect"] (lineage row-quality flag per LINEAGE-01; distinct from `qc_status` enum slot AND distinct from the `obs_qc_status` bitmask column per QC-05)
- (source enum extended to add "ncei" — never written in v0.1.0)
```

Note: do NOT modify `qc_status` — it remains the per-rule QC enum slot (separate concept from `observation_quality`). Per the iteration-4 fix to ROADMAP line 179, the bitmask lives in `obs_qc_status`, but PLAN-01's interfaces already correctly lists `qc_status` as a string enum (a different column). The 9th column being added here, `observation_quality`, is also a string enum but with different semantics (row-quality, not rule-firing). The parenthetical note inside the new_string makes this distinction explicit so future readers don't conflate the three column names.

Also: do NOT add `observation_quality` to the `required` array in any JSON Schema file — like the other 8 lineage fields, it's nullable/additive.

Do NOT modify any other line in PLAN-01. The Task 1 action body around lines 140-160 already constructs the JSON Schema generously and will be updated when PLAN-01 is executed; this quick task only fixes the planning prose.
  </action>
  <verify>
    Run: `grep -c "observation_quality" .planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-01-schema-and-validator.md` — expected: at least 2 hits (Sub-edits A and B). Also: `grep -c "\`observation_quality\`" .planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-01-schema-and-validator.md` should return at least 1 (the objective sentence cites it in backticks). And: `grep -c "string enum \\[\"clean\", \"flagged\", \"suspect\"\\]" .planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-01-schema-and-validator.md` should return at least 1 (Sub-edit B's interface bullet).
  </verify>
  <done>
- PLAN-01 objective sentence (line 55) lists 9 lineage columns by name, including `observation_quality`.
- PLAN-01 interfaces block (lines 87-95) has an `observation_quality` bullet with the lineage enum values and a parenthetical distinguishing it from `qc_status` and `obs_qc_status`.
- No other lines modified.
- Commit message: `docs(02.1-PLAN-01): add 9th lineage column observation_quality (REVIEW iter-5 F5)`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor -> .planning/ files | Doc edits only; no executable code touched |
| executor -> git index | Five separate commits; user owns merge decision (no push in this plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-lz3-01 | T (Tampering) | Edit lands on wrong file (e.g. PLAN-03 edit applied to PLAN-04) | mitigate | Each task's `<files>` block lists exactly one target path; the action's Read step confirms the current text before Edit. Verify block greps the target file specifically. |
| T-lz3-02 | T (Tampering) | Task 1 (Finding 1) restructure leaves dangling references in PLAN-04 (e.g. comments still mention the deleted single-builder) | mitigate | Task 1 verify checks BOTH that new helpers exist AND that the old `_hardcoded_silver_with_duplicates` name returns 0 hits. Task 3 verify checks the old 12/18 counts also return 0 hits. |
| T-lz3-03 | I (Information disclosure) | Diff accidentally includes source code (packages/) | mitigate | `files_modified` frontmatter lists only `.planning/` paths; each task's `<files>` lists only `.planning/` paths; reviewer can `git diff --stat` to confirm. |
| T-lz3-04 | E (Elevation) | A future plan revision relies on a stale test count or stale lock pattern claim from PLAN-03/04 | mitigate | Task 3 explicitly converges three sites to ONE source of truth (Task 1 verify, Task 1 acceptance, plan-level verification). Task 2 unifies the lock pattern at both the action site (step 4e) and the threat register (T-02.1.3-02). |
| T-lz3-05 | R (Repudiation) | Five fixes land in a single squashed commit, losing per-finding traceability | mitigate | Each task's `<done>` block specifies a commit message of the form `docs(<file>): <one-line summary> (REVIEW iter-5 F<n>)`. Executor commits five times, one per task. |
</threat_model>

<verification>
After all 5 tasks complete, run these from the worktree root to confirm every fix landed cleanly:

```bash
# Finding 1: three single-class helpers + three single-class tests in PLAN-04
grep -c "_hardcoded_silver_class_a_cross_priority\|_hardcoded_silver_class_b_same_priority_later_ts\|_hardcoded_silver_class_c_same_priority_null_ts" .planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-04-integration-parity-gate.md
# expect: at least 6 (3 helper defs + 3 test invocations)
grep -c "_hardcoded_silver_with_duplicates\|test_pre_flight_strict_priority_with_synthetic_duplicates" .planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-04-integration-parity-gate.md
# expect: 0 (old helper + old test deleted)
grep -c "def test_pre_flight_class_[abc]_" .planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-04-integration-parity-gate.md
# expect: 3

# Finding 2: unified lock pattern in PLAN-03
grep -c "ledger_p.parent" .planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-03-cache-layer-refactor.md
# expect: 0
grep -c "ledger_p.with_suffix\|per-file \`.lock\` sibling" .planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-03-cache-layer-refactor.md
# expect: at least 2 (action step 4e + T-02.1.3-02)
grep -c "test_auto_upgrade_does_not_race_with_concurrent_write_cache" .planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-03-cache-layer-refactor.md
# expect: at least 1
grep -c "19 tests PASSED\|10 + 9 + 8 = 27" .planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-03-cache-layer-refactor.md
# expect: at least 2

# Finding 3: PLAN-04 test counts reconciled
grep -c "18 tests PASSED\|12 tests PASSED\|7 + 5 + 3 + 2 + 1 = 18" .planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-04-integration-parity-gate.md
# expect: 0
grep -c "22 tests PASSED\|22 + 5 + 3 + 2 + 1 = 33" .planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-04-integration-parity-gate.md
# expect: at least 2

# Finding 4: STATE.md total_plans bumped
grep "^  total_plans:" .planning/STATE.md
# expect: "  total_plans: 15"
grep -c "total_plans: 13" .planning/STATE.md
# expect: 0

# Finding 5: observation_quality in PLAN-01
grep -c "observation_quality" .planning/phases/02.1-sprint-2o-lineage-refactor-per-source-provenance/02.1-PLAN-01-schema-and-validator.md
# expect: at least 2

# Source code untouched
git diff --stat | grep -E "^ packages/" || echo "OK: no packages/ changes"
# expect: "OK: no packages/ changes" line printed

# Five separate commits expected on the branch (each task = one commit per <done> block)
git log --oneline -5 | grep -cE "REVIEW iter-5 F[1-5]"
# expect: 5 (if executor committed one per task per the <done> guidance)
```
</verification>

<success_criteria>
- All 5 iteration-5 findings closed via surgical text edits to 4 planning files.
- Five commits on `planning/v01-intl-nwp-polymarket` (one per finding), each with `(REVIEW iter-5 F<n>)` suffix for traceability.
- No source code changes (no `packages/` files in the diff).
- No new YAML keys introduced in STATE.md.
- The three PLAN-04 test-count sites converge on the same number after Task 3.
- The two PLAN-03 lock-acquisition sites (action step + threat register) cite the same FileLock target.
- The PLAN-01 objective and interfaces block both name `observation_quality` as the 9th lineage column.
- Pre-commit hooks pass without `--no-verify`.
</success_criteria>

<output>
After completion, append a one-line entry to STATE.md's "Quick Tasks Completed" table:
```
| 260522-lz3 | Fix 5 REVIEW-DISCIPLINE iteration 5 findings | 2026-05-22 | <commit-sha-of-task-5> | [260522-lz3-fix-5-review-discipline-iteration-5-find](./quick/260522-lz3-fix-5-review-discipline-iteration-5-find/) |
```

This is a follow-up bookkeeping step — the executor can do it after all 5 commits land. The user decides when (and whether) to merge `planning/v01-intl-nwp-polymarket` into `merged-vision`; no merge or push is in scope for this plan.
</output>
