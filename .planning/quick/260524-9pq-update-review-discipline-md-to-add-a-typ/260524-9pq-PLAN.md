---
phase: 260524-9pq
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REVIEW-DISCIPLINE.md
autonomous: true
requirements:
  - TS-BUNDLE-01
  - TS-CORS-01

must_haves:
  truths:
    - "REVIEW-DISCIPLINE.md routes Python-only PRs to codex `high` + Python Architect (status quo preserved verbatim)"
    - "REVIEW-DISCIPLINE.md routes TypeScript-only PRs to codex `high` + TypeScript Architect"
    - "REVIEW-DISCIPLINE.md routes mixed-language PRs (Python + TS in same diff) to codex `high` + Python Architect + TypeScript Architect, with the loop continuing until ALL three return clean"
    - "A TypeScript Architect rubric subsection exists covering type safety, bundle-size impact (per TS-BUNDLE-01 caps), browser/MV3 compatibility (per TS-CORS-MATRIX.md / TS-CORS-01), API surface parity with Python (per PYTHON-SURFACE-INVENTORY.md + CROSS-SDK-SYNC.md §2.1), and TS idioms"
    - "Existing discipline preserved verbatim: CRITICAL/HIGH-only severity gate, 3-iteration smell-escalate rule, never-skip path list, trivial-skip rules"
    - "The CROSS-SDK-SYNC.md §8 open question #3 (ts-architect reviewer role) is resolved by this edit"
  artifacts:
    - path: .planning/REVIEW-DISCIPLINE.md
      provides: "Updated two-reviewer loop with language-routing matrix + TS Architect rubric"
      contains: "TypeScript Architect"
  key_links:
    - from: ".planning/REVIEW-DISCIPLINE.md"
      to: ".planning/research/TS-SDK-DESIGN.md"
      via: "rubric reference"
      pattern: "TS-SDK-DESIGN"
    - from: ".planning/REVIEW-DISCIPLINE.md"
      to: ".planning/research/PYTHON-SURFACE-INVENTORY.md"
      via: "rubric reference for API parity check"
      pattern: "PYTHON-SURFACE-INVENTORY"
    - from: ".planning/REVIEW-DISCIPLINE.md"
      to: ".planning/CROSS-SDK-SYNC.md"
      via: "rubric reference for parity-ticket discipline (§2.1)"
      pattern: "CROSS-SDK-SYNC"
    - from: ".planning/REVIEW-DISCIPLINE.md"
      to: ".planning/REQUIREMENTS.md"
      via: "rubric references TS-BUNDLE-01 + TS-CORS-01"
      pattern: "TS-BUNDLE-01|TS-CORS-01"
---

<objective>
Update `.planning/REVIEW-DISCIPLINE.md` to add a TypeScript Architect reviewer alongside the existing Python Architect, with a language-routing matrix that selects the right reviewer pair per PR diff.

Purpose: TS-W1 (Chrome-extension MVP) already shipped under the current loop, but the iteration-1 findings show TS-specific issues (CORS posture, MV3 service-worker constraints, bundle size, Python-SDK surface parity) that a Python-Architect reviewer is not optimally calibrated for. As TS-W2 and beyond land — and as mixed-language PRs (Python lift + TS port) become routine — the loop needs explicit routing. This edit also closes CROSS-SDK-SYNC.md §8 open question #3 (ts-architect reviewer role).

Output: Updated `.planning/REVIEW-DISCIPLINE.md` with a new "Language routing" subsection inserted into the two-reviewer loop, a new "TypeScript Architect rubric" subsection mirroring the Python Architect implicit rubric, and a Lineage-entry note that the open-question §3 from CROSS-SDK-SYNC.md is resolved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REVIEW-DISCIPLINE.md
@.planning/CROSS-SDK-SYNC.md
@.planning/research/TS-SDK-DESIGN.md
@.planning/research/PYTHON-SURFACE-INVENTORY.md
@.planning/research/TS-CORS-MATRIX.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update REVIEW-DISCIPLINE.md with language-routing matrix + TS Architect rubric</name>
  <files>.planning/REVIEW-DISCIPLINE.md</files>
  <action>
Edit `.planning/REVIEW-DISCIPLINE.md` to add TypeScript Architect routing and rubric. Make the following surgical changes; preserve all existing text verbatim except the additions called out below.

**1. Insert a new subsection `## Language routing` immediately AFTER the existing `## The two-reviewer loop` section and BEFORE `## When to skip the loop`.** Use this exact content (Markdown):

```markdown
## Language routing

The two-reviewer loop above is universal. The IDENTITY of the second reviewer (codex `high` is always the first) is selected by the PR's language footprint:

| PR diff touches | Reviewer pair |
|---|---|
| **Python only** — `packages/core/`, `packages/weather/`, `packages/markets/`, `tests/`, `scripts/*.py`, `.github/workflows/*python*`, or Python-only docs (`docs/**.md` referencing Python APIs) | codex `high` + **Python Architect** |
| **TypeScript only** — `packages-ts/**`, `.github/workflows/*ts*` (e.g. `test-ts.yml`, `release-ts.yml`, `drift-rotate-ts.yml`), or TS-only docs (anything under `packages-ts/examples/**`, which counts as language-neutral docs) | codex `high` + **TypeScript Architect** |
| **Mixed** — Python source AND TS source in the same PR (e.g. a Python lift paired with its TS port per CROSS-SDK-SYNC.md §6.1 Recipe A) | codex `high` + **Python Architect** + **TypeScript Architect** running in parallel; loop continues until ALL THREE return clean (no CRITICAL/HIGH) |

**Language-neutral diffs** (root `README.md`, `LICENSE`, `.gitignore`, top-level `.planning/` docs that don't include code/schema fragments, `pyproject.toml`/`package.json` workspace-root metadata without dependency floor changes) follow the trivial-skip rules below regardless of which architect would otherwise route. The never-skip path list still applies — a `pyproject.toml` dependency floor change or a `.planning/CROSS-SDK-SYNC.md` edit containing code-like fragments triggers the loop irrespective of language footprint.

**Schemas + codegen edge case.** A PR that touches `scripts/export_schemas.py` + `schemas/` + `packages-ts/*/src/**/generated/` (the canonical Python→TS codegen flow per CROSS-SDK-SYNC.md §1) routes as **mixed** — the Python Architect reviews the exporter changes and the TypeScript Architect reviews the consumed shape (generated `.d.ts`, runtime validators, typed station/Kalshi imports).
```

**2. Insert a new subsection `## TypeScript Architect rubric` immediately AFTER the existing `## Reviewer prompt rules` section and BEFORE `## Lineage`.** Use this exact content:

```markdown
## TypeScript Architect rubric

The TypeScript Architect is a Claude general-purpose agent with the "Senior TypeScript Architect" persona — brutal about correctness, idioms, type safety, and bundle/browser-runtime constraints. Same severity-gate discipline as the Python Architect (CRITICAL or HIGH only; no MEDIUM/LOW; no style nits). The reviewer is told to read the diff against the rubric below and emit a PASS or REVISE verdict.

Focus areas (each finding cites the bucket it falls into):

**1. Type safety.**
- `strict: true` plus the four mandated `tsconfig` flags from TS-SDK-DESIGN §3.1 (`strict`, `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`, `verbatimModuleSyntax`) hold — no per-file or per-line opt-outs.
- No unjustified `any` / `unknown` casts. `as X` casts have a comment explaining why the type system can't prove it. Type-guards (`function isX(v: unknown): v is X`) preferred over assertions when the predicate is non-trivial.
- Discriminated unions preferred over runtime tag-checking with `if ('foo' in obj)`. Exhaustive `switch` on discriminants via `const _exhaustive: never = x;` (or equivalent `assertNever` helper).
- Narrowing is explicit at API boundaries (don't smuggle wider types through).

**2. Bundle size impact.**
- Per-package `size-limit` gates from **TS-BUNDLE-01** hold: `@tradewinds/core` ≤ 25 KB, `@tradewinds/weather` ≤ 35 KB, `@tradewinds/markets` ≤ 10 KB, `tradewinds` meta ≤ 70 KB (all min+gzip). PRs that materially grow a bundle MUST show the size-limit report in the PR description; HIGH if a gate is breached without justification.
- No accidental tree-shake breakers: top-level side effects in package source (run-on-import code, top-level `await`, mutating module-scope state), dynamic-key `require` / dynamic `import(name)` where `name` isn't statically analyzable, broad re-export barrels that pull in heavy modules unconditionally.
- No `import *` from heavy deps (e.g. `import * as arrow from 'apache-arrow'` instead of named imports). Optional adapters (Arrow, parquet-wasm) MUST stay behind `peerDependenciesMeta: { optional: true }` and a runtime feature-detect, never a top-level import.

**3. Browser / MV3 compatibility.**
- MV3 service-worker constraints: no `eval`, no `new Function(...)`, no remote code loading; CSP-safe in `script-src 'self'`. `ajv` runtime validators MUST be the `ajv-standalone` precompiled form (per TS-SDK-DESIGN §4.3) — never runtime `ajv` in shipped bundles.
- No Node-only APIs (`node:fs`, `node:path`, `node:crypto.randomBytes`, `node:os`, `node:net`, `process.cwd()`) in the `@tradewinds/core` or `@tradewinds/weather` shared surface. Node-only code (e.g. `FsStore`) lives behind a runtime feature-detect (`typeof process !== 'undefined' && process.versions?.node`) per TS-SDK-DESIGN §5.4, never a top-level import.
- CORS posture preserved per `.planning/research/TS-CORS-MATRIX.md` (per **TS-CORS-01**). If a fetcher is changed to call a new upstream endpoint or change a request shape, CORS posture is re-verified empirically and the matrix updated in the same PR (or a parity ticket filed). HIGH if a new fetch is added with no matrix entry.

**4. API surface parity with Python SDK.**
- Function signatures, return shapes, error types, and casing rules match `.planning/research/PYTHON-SURFACE-INVENTORY.md` for any surface item the TS port covers. Deviations (additive widening, parameter rename, return-shape change, exception class swap) require a CROSS-SDK-SYNC parity ticket per `.planning/CROSS-SDK-SYNC.md` §2.1 — HIGH if the PR introduces drift without a `Parity-Ticket:` line in the PR body or a paired Python diff in the same PR.
- Codegen-shared types (`schemas/json/*.json` → `packages-ts/*/src/**/generated/*.d.ts`) are NOT hand-edited; the rubric calls HIGH on any direct edit to `generated/` (per CROSS-SDK-SYNC.md §7 anti-patterns).
- Per-row source identity / source-mismatch semantics (Python `SourceMismatchError`) are preserved in the TS exception class hierarchy. Public exception classes carry a `toDict()` method analogue (`{ name, message, ...payload }`) for future MCP-client serialization per TS-SDK-DESIGN §1 non-goals (MCP client deferred; serialization shape stays compatible).

**5. TypeScript idioms.**
- Prefer `readonly` / `Readonly<T>` / `ReadonlyArray<T>` on public contracts (rows, options, returned envelopes). Mutating shared rows post-return is a HIGH.
- Named function exports over default exports across all package source (default exports break IDE rename, hide barrels' re-exports, and complicate tree-shaking).
- ESM-first: no CJS-only patterns (`module.exports`, `require()`) in package source. The dual ESM/CJS dist is produced by `tsup`; source stays ESM.
- Avoid `enum` in favor of `const` unions (`as const` arrays + `typeof X[number]` derived unions) — enums emit runtime code that defeats tree-shaking and don't survive `verbatimModuleSyntax`.
- Errors thrown across the API surface are subclasses of the relevant `TradewindsError` analogue, never bare `Error` or `string`.

**Calibration examples.** Same severity gate as Python Architect: CRITICAL = silent data corruption / broken invariant / parity-critical regression; HIGH = meaningful design issue OR test that won't catch what it claims. Examples specific to TS:

- TS adapter emits `source: "awc.live"` while Python emits `"awc"` (or vice-versa), without a parity ticket → CRITICAL (silent per-row source-identity drift; the iter-1 finding from TS-W1 review).
- Top-level `await fetch(...)` added to `@tradewinds/core/src/index.ts` → CRITICAL (breaks MV3 service-worker import + balloons bundle).
- `as any` cast on a row at the public boundary of `research()` → HIGH (smuggles untyped data through a typed contract).
- `import * as luxon from 'luxon'` in `@tradewinds/core` when only `DateTime.fromISO` is needed → HIGH (bundle-size impact; named import required).
- `enum Source { AWC = 'awc', IEM = 'iem' }` added to a generated types file → HIGH (defeats tree-shaking; use `const` union).
- `// @ts-expect-error` with no comment → HIGH (untracked type debt).
- Prettier-ish formatting nit (`,` vs `;` at end of interface line) → skip.
- "Could rename `fetchObs` to `fetchObservations` for clarity" → skip (style nit unless it conflicts with PYTHON-SURFACE-INVENTORY name).
```

**3. Append one bullet to the `## Lineage` section** (immediately after the existing two bullets, BEFORE the file ends) noting the resolution of `.planning/CROSS-SDK-SYNC.md` §8 open question #3:

```markdown
- 2026-05-24: TypeScript Architect reviewer added alongside Python Architect via the language-routing matrix above. Resolves [`CROSS-SDK-SYNC.md` §8 open question #3](./CROSS-SDK-SYNC.md#8-open-questions-resolve-before-ts-w0-ships) ("ts-architect reviewer role — likely a `ts-architect` Claude-Code agent or similar second-pair reviewer; decision deferred to TS-W0; REVIEW-DISCIPLINE.md updated then"). The TS Architect rubric mirrors the Python Architect's severity-gate discipline (CRITICAL/HIGH only; no nits) and is calibrated against TS-SDK-DESIGN §1 (goals/non-goals), §3.1 (mandated tsconfig flags), §4.3 (codegen + ajv-standalone), §5 (browser/CORS), and `.planning/research/PYTHON-SURFACE-INVENTORY.md` (API parity).
```

**4. Verify nothing else changed.** Do NOT rewrite, reflow, or "improve" any of the existing sections (`## The two-reviewer loop`, `## When to skip the loop`, `## Severity examples (calibration)`, `## Reviewer prompt rules`, `## Lineage` — except the appended bullet, the existing two bullets stay verbatim). Severity gate stays CRITICAL/HIGH only; 3-iteration smell-escalate rule stays; never-skip path list stays unchanged; trivial-skip rules stay unchanged. The mixed-language rule explicitly says all three reviewers must return clean — that's a strengthening of the loop, not a loosening.
  </action>
  <verify>
    <automated>
test -f .planning/REVIEW-DISCIPLINE.md && \
grep -q "^## Language routing$" .planning/REVIEW-DISCIPLINE.md && \
grep -q "^## TypeScript Architect rubric$" .planning/REVIEW-DISCIPLINE.md && \
grep -q "codex \`high\` + \*\*Python Architect\*\* + \*\*TypeScript Architect\*\*" .planning/REVIEW-DISCIPLINE.md && \
grep -q "TS-BUNDLE-01" .planning/REVIEW-DISCIPLINE.md && \
grep -q "TS-CORS-01" .planning/REVIEW-DISCIPLINE.md && \
grep -q "PYTHON-SURFACE-INVENTORY" .planning/REVIEW-DISCIPLINE.md && \
grep -q "CROSS-SDK-SYNC.md" .planning/REVIEW-DISCIPLINE.md && \
grep -q "TS-SDK-DESIGN" .planning/REVIEW-DISCIPLINE.md && \
grep -q "ajv-standalone" .planning/REVIEW-DISCIPLINE.md && \
grep -q "MV3" .planning/REVIEW-DISCIPLINE.md && \
grep -q "## The two-reviewer loop" .planning/REVIEW-DISCIPLINE.md && \
grep -q "## When to skip the loop" .planning/REVIEW-DISCIPLINE.md && \
grep -q "## Severity examples (calibration)" .planning/REVIEW-DISCIPLINE.md && \
grep -q "## Reviewer prompt rules" .planning/REVIEW-DISCIPLINE.md && \
grep -q "## Lineage" .planning/REVIEW-DISCIPLINE.md && \
grep -q "iteration count reaches 3" .planning/REVIEW-DISCIPLINE.md && \
grep -q "review-skip: trivial" .planning/REVIEW-DISCIPLINE.md && \
echo "VERIFY OK"
    </automated>
  </verify>
  <done>
    - `.planning/REVIEW-DISCIPLINE.md` contains a `## Language routing` section with the three-row routing matrix (Python-only → codex+PA; TS-only → codex+TSA; mixed → codex+PA+TSA all-must-pass).
    - `.planning/REVIEW-DISCIPLINE.md` contains a `## TypeScript Architect rubric` section with five focus areas (type safety, bundle size, browser/MV3, API parity, TS idioms) and calibration examples.
    - The TS rubric explicitly cites TS-BUNDLE-01 caps (25/35/10/70 KB) and TS-CORS-01 (TS-CORS-MATRIX.md) by name.
    - The TS rubric cross-references PYTHON-SURFACE-INVENTORY.md and CROSS-SDK-SYNC.md §2.1.
    - Existing sections (two-reviewer loop, skip rules, severity examples, reviewer prompt rules, original lineage bullets) are byte-preserved except for the appended lineage bullet.
    - Lineage section gained one new bullet noting resolution of CROSS-SDK-SYNC.md §8 open question #3.
    - All `grep -q` checks in the verify block pass and the command prints `VERIFY OK`.
  </done>
</task>

</tasks>

<verification>
After Task 1 completes, run:

```bash
# 1. The automated verify block from Task 1.
test -f .planning/REVIEW-DISCIPLINE.md && \
grep -q "^## Language routing$" .planning/REVIEW-DISCIPLINE.md && \
grep -q "^## TypeScript Architect rubric$" .planning/REVIEW-DISCIPLINE.md && \
grep -q "TS-BUNDLE-01" .planning/REVIEW-DISCIPLINE.md && \
grep -q "TS-CORS-01" .planning/REVIEW-DISCIPLINE.md && \
echo "verify ok"

# 2. Confirm preservation of existing structure — same five top-level sections plus two new.
grep -nE "^## " .planning/REVIEW-DISCIPLINE.md
# Expected output, in order:
#   ## The two-reviewer loop
#   ## Language routing            (NEW)
#   ## When to skip the loop
#   ## Severity examples (calibration)
#   ## Reviewer prompt rules
#   ## TypeScript Architect rubric (NEW)
#   ## Lineage

# 3. Sanity-check no accidental removal of "never skip" path enumerations.
grep -c "Never skip" .planning/REVIEW-DISCIPLINE.md   # expect >= 1
grep -c "trivial" .planning/REVIEW-DISCIPLINE.md       # expect >= 2 (existing + lineage references)
```
</verification>

<success_criteria>
1. **Routing matrix exists and is correct.** The three rows (Python-only, TS-only, mixed) are present with the right reviewer pairs; mixed explicitly requires ALL THREE reviewers to return clean.
2. **TS Architect rubric covers all five focus areas.** Type safety (with the four mandated tsconfig flags), bundle size (with TS-BUNDLE-01 numeric caps), browser/MV3 compatibility (with TS-CORS-01 cross-reference), API surface parity (with PYTHON-SURFACE-INVENTORY + CROSS-SDK-SYNC §2.1 cross-references), TS idioms (readonly, named exports, ESM, const unions, error subclassing).
3. **Existing discipline preserved verbatim.** Severity gate (CRITICAL/HIGH only) unchanged; 3-iteration smell-escalate rule unchanged; never-skip path list unchanged; trivial-skip rules unchanged. The mixed-language rule strengthens (not loosens) the loop.
4. **Cross-doc resolution noted.** Lineage section gains a bullet noting that CROSS-SDK-SYNC.md §8 open question #3 (ts-architect reviewer role) is resolved by this edit.
5. **Section ordering preserved.** Top-level section sequence is: two-reviewer loop → Language routing (NEW) → skip rules → severity examples → reviewer prompt rules → TS Architect rubric (NEW) → Lineage.
</success_criteria>

<output>
After completion, create `.planning/quick/260524-9pq-update-review-discipline-md-to-add-a-typ/260524-9pq-SUMMARY.md` documenting:
- The edit landed (file path + section additions)
- The two new section names + their positions
- Confirmation that no existing prose was rewritten
- Note that this resolves CROSS-SDK-SYNC.md §8 open question #3 (next CROSS-SDK-SYNC.md revision should strike that open question and reference the REVIEW-DISCIPLINE.md update by commit SHA)
- Suggested follow-up: open a tiny PR to also strike question #3 from CROSS-SDK-SYNC.md §8 in a subsequent commit (NOT this PR — keep this PR focused)
</output>
